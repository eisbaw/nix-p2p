---
id: TASK-102
title: KISS public-NAR allowlist learned from cache.nixos.org requests
status: Done
assignee:
  - '@claude'
created_date: '2026-08-10 10:03'
updated_date: '2026-08-14 14:44'
labels:
  - privacy
  - publication
  - allowlist
  - iroh
  - blocking
  - wave-2b
dependencies:
  - TASK-115
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Implement one persistent append-only local allowlist. When the daemon handles a NAR request and the normal exact-key cache.nixos.org narinfo response cryptographically proves that NAR identity public the daemon appends its NarHash and NarSize once. Do not scan the whole Nix store and do not make a separate discovery crawl. Every DHT or other public publisher must consult this single list; absence rejects publication. Local-only unsigned content never enters it.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 One PublicNarAllowlist enforcement point owns append and contains checks and every public publisher can consume only its approved entries.
- [ ] #2 The existing requested-narinfo path appends one canonical NarHash and NarSize record only after an exact cache.nixos.org response is correlated and its trusted Nix signature is verified. Duplicate requests are idempotent and require no second network request or store census.
- [ ] #3 MISS outage timeout malformed metadata bad signature wrong authority hash mismatch size mismatch local build and private upstream evidence append nothing and return named fail-closed outcomes. Mutations neutralizing each guard fail.
- [ ] #4 The list is restart-persistent append-only bounded and crash-safe with strict file owner mode type link and record parsing. A torn final append loses at most that uncommitted entry and never creates eligibility.
- [ ] #5 Status reports allowlisted count and total NarSize without StorePath NarHash or inventory labels. The file contains no StorePath and there is no remote enumeration API; documentation warns that publication still fingerprints public package holdings.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. daemon-core/src/public_allowlist.rs: (a) Nix narinfo ed25519 Sig verification over fingerprint 1;StorePath;NarHash;NarSize;References against configured TRUSTED keys (name->ed25519 pubkey); (b) PublicNarAllowlist = single enforcement point: verify->append-once (idempotent), contains(NarHashKey), status(count,total NarSize) with NO StorePath/inventory; (c) append-only crash-safe line-file persistence with strict file owner/mode/type/link + strict record parse, torn tail dropped.
2. Wire APPEND at server.rs::respond_narinfo verified-public moment (AC#2), idempotent, no 2nd network req.
3. Publication gate consult API (contains + gate helper), fail-closed named outcomes; seam test un-allowlisted->reject. Wiring into shipped announce loops routed to mped-architect; forward-carry TASK-103.
4. Fail-closed guards (AC#3) each bite by mutation: MISS/outage/timeout(never reach append), malformed metadata, bad sig, wrong authority, hash/size tamper, unsigned(local build/private). 5. no-enumeration + doc-citation preserved (AC#5).
Deps: add ed25519-dalek=3, base64=0.22 to daemon-core (not on independence denylist).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Owner KISS decision 2026-08-11: append requested NARs proven public by cache.nixos.org to one local list. No census. Deferred briefly behind the cornerstone TASK-126 DHT core; TASK-103 integration still requires this gate.

Implemented (commits c67aab2 core module; 5cf73ec append-site wiring; b65263e typed public door). AC1 PublicNarAllowlist single writer(learn)+reader(approve minting unforgeable PublicNarClaim); AC2 append at server.rs::respond_narinfo on the exact upstream response, idempotent, only after verify; AC3 6 named fail-closed guards, bite-by-mutation demonstrated (verify_strict neuter->4 red; authority neuter->9 red; publish-gate neuter->2 red); AC4 FileAllowlistStore append-only O_APPEND+sync_all+dir-fsync, torn-tail dropped, corrupt-committed fails loud, strict owner/mode/type/link; AC5 status=count+total NarSize only, no StorePath in file, no list/iter/remote API, no_enumeration.rs scans the module. Gate: daemon-core+daemon 395 pass, daemon-libp2p lib 11, no_enumeration 11, doc_citations 3, independence green, fmt+clippy clean. LIMITS: allowlist disabled() by default in shipped daemons (no trusted-keys/path CLI yet) so learning inert until TASK-103 wires config; public announce door has no shipped caller yet (TASK-103). HONEST: publishing still fingerprints public holdings (documented; axis-6 leakage is a separate gate).

DEEP GATE 2026-08-14 = codex NO-GO (crypto core sound: verify_strict + sig-covered NarHash/NarSize; the POLICY BOUNDARIES fail). SECURITY FIX CYCLE: (1) BLOCKING gate is docs not a type boundary — announce_public_provisions (daemon-libp2p/src/lib.rs:789) calls the allowlist then DISCARDS claims + calls the ungated announce_store_provisions (:708); shipped provider paths call the ungated publisher DIRECTLY (daemon-libp2p/src/main.rs:455 + :352 seed; daemon/src/main.rs:1574 + :1420 seed). FIX: make the public announce path STRUCTURALLY require a PublicNarClaim per announced NAR (consume, not discard) and route every SHIPPED public publisher through it; a truly-private/LAN loop must be a DISTINCT typed path, not the same ungated call. If the typed public-vs-private substrate split is large, do the minimum: the public/kad announce requires claims, and any remaining ungated caller is proven non-public or filed with a precise note. (2) BLOCKING claim forgeable via persistence seam — AllowlistStore is pub + re-exported (lib.rs:70); PublicNarAllowlist::open (:677) accepts ANY impl and trusts every load() entry; FileAllowlistStore::append is publicly callable. FIX: SEAL the store — make AllowlistStore/open/append pub(crate) or a sealed trait so external code cannot inject entries then approve() a forged claim; approve() must only mint from learn-verified state. (3) BLOCKING persistence corruption fabricates eligibility — every syntactically-valid line trusted on load (public_allowlist.rs:519), no checksum/MAC; flip one nixbase32 char -> a never-learned NAR is eligible after restart (violates AC#4). FIX: integrity-protect the persisted log with a KEYED MAC (per-node secret, e.g. derived from the durable identity seed) so a modified/injected line fails on load; TRUNCATE a torn tail (:500) instead of concatenating; write an integrity commit marker (not a bare newline, :547); do NOT silently discard parent-dir fsync errors (:558). (4) HIGH exact-request correlation absent — respond_narinfo doesn't receive the requested StoreHash; learning (server.rs:289) happens before correlation is parsed (:308). FIX: pass the requested StoreHash and append ONLY when the signed narinfo is the correlated response to the EXACT requested key (URL/StorePath match); reject missing-URL/duplicate-field; unify first-vs-last field semantics (proof uses first :235, correlation uses last catalog.rs:125). The integration fake (public_allowlist_learn.rs:42) ignores the requested key — fix it so the test can catch a mismatched-append. (5) HIGH strict-file — require st_nlink==1, mode 0600 (not 0644, the NarHash inventory must not be world/group-readable), validate parent-dir ownership/mode + no symlinked parent, add a file-size/record-count bound + bounded read (not read_to_end), and a cross-process writer lock. (6) HIGH Nix canonicalization — fingerprint the PARSED/canonical NarHash + DECIMAL NarSize + Nix's reference-set ordering (not raw spelling/input order); reject structurally-invalid narinfo (missing URL, missing final newline, duplicate fields, zero NarSize) the way Nix's parser does. Each fix needs a mutation test that BITES (the forged-store, the flipped-char, the mismatched-correlation, the ungated-publisher each currently PASS - they must go RED). NarSize stays integer (no-float rule). Keep the crypto core (verify_strict, sig-covered append). Report each blocker before(passes)/after(rejected).

SECURITY FIX CYCLE (codex DEEP NO-GO -> fixed). Crypto core kept (verify_strict, sig-covered NarHash/NarSize, integer NarSize). (1) BLOCKING ungated publisher: public door now consumes a load-bearing capability ApprovedPublicProvision (verified StoreProvision + minted PublicNarClaim); private/LAN loops retyped to require a LanShare witness (announce_provider_seeds/announce_store_provisions); shipped provider modes gated by lan_share_or_refuse (non-empty bootstrap + unconfigured allowlist -> REFUSE naming TASK-103; empty bootstrap = isolated LAN still runs). Per mped Option-B arbitration; residual filed as HARD BLOCKER on TASK-103. Bite: bootstrap-guard test goes RED when neutered. (2) BLOCKING forged-store: AllowlistStore trait + open + FileAllowlistStore/NullAllowlistStore sealed pub(crate) + dropped from re-exports; new sealed ctors in_memory/open_file. Bite: external forged-store attack now fails to compile (E0405). (3) BLOCKING persistence integrity: per-record keyed-blake3 MAC under a per-node key derived from the identity seed (derive_allowlist_mac_key); flipped/injected/foreign line fails LOUD on load; torn tail truncated (not concatenated) before append; parent-dir fsync error surfaced; flock writer lock. Bite: flipped-char + foreign-key tests go RED when MAC check neutered. (4) HIGH exact-request correlation: learn(requested_hash, narinfo) rejects a signed narinfo whose StorePath != requested key (LearnOutcome::RequestMismatch); server threads the requested StoreHash; the integration fake now exercises request-A-receive-B. parse_correlation unified to first-occurrence + duplicate-reject (matches prove_public). Bite: request-mismatch unit + seam tests go RED when neutered. (5) HIGH strict file: st_nlink==1, mode 0600 (0o077 refused), parent-dir owner/mode + O_NOFOLLOW symlink refusal, file-size + record-count bounds, bounded read, flock. (6) HIGH Nix canonicalization: fingerprint over parsed canonical NarHash + decimal NarSize + SORTED refs; reject missing-URL, missing-final-newline, duplicate fields, zero NarSize. Gate: cargo build + test (daemon-core/daemon/daemon-libp2p all green), fmt, clippy -D warnings, just independence, no_enumeration (parser fixed to attribute pub(crate) trait), doc_citations all green. LIMITS: parent-dir check is immediate-parent only (no full ancestor symlink walk); bootstrap guard covers announce out-reach only (inbound serve is a separate seam); allowlist still disabled-by-default until TASK-103 wires config.

RE-GATE 2026-08-14 (codex, partial — my waiter tooling misfired 3x: hang, loose-regex, self-match-kill; findings still captured): #2 CONFIRMED resolved (store sealed pub(crate), forged-store fails to compile E0405). #1 RESIDUAL BLOCKER (real, my tests missed it): the thin daemon-libp2p provider started with --libp2p-provider-addr and EMPTY --libp2p-bootstrap still joins the public kad DHT (the provider-addr enters the routing table), but lan_share_or_refuse checks ONLY the empty bootstrap vector -> mints LanShare -> announce_provider_seeds/announce_store_provisions call start_providing/put_record UNGATED. The guard checks the wrong signal: bootstrap-empty != isolated-LAN. #3/#4/#5/#6 pass my independent bite tests but codex was killed before fully probing them. FIX CYCLE #2: (A) lan_share_or_refuse must refuse announcing whenever the node can reach ANY public substrate without a configured+enforcing allowlist — check ALL public-reach signals, not just bootstrap: --libp2p-provider-addr / dial-addr overrides, relay, explicit dials, any non-loopback listen/dial. A LanShare witness must require POSITIVE LAN-only isolation (loopback/link-local only, no public-reach config), not merely absence-of-bootstrap. Bite test: a provider with --libp2p-provider-addr + empty bootstrap + no allowlist must REFUSE to announce (currently it announces). (B) DEFENSIVE hardening of the codex probe items it didn't finish: MAC — add bite tests for replay (duplicate a valid MAC'd line), swap (NarHash<->NarSize within a record), and a line MAC'd under a DIFFERENT node's key; each must fail loud on load. Correlation — bite tests for request-A-receive-signed-B (RequestMismatch), missing-URL, duplicate-field. If any currently passes, fix it. Keep everything from fix cycle #1 (sealed store, per-record MAC, consumed claim, 0600/nlink, canonicalization). NarSize integer. The residual (real allowlist enforcement replacing the isolation guard) stays TASK-103's hard blocker.

FIX CYCLE #2 DONE (commits 4f173c5 Fix A, 0872966 Fix B). FIX A (BLOCKING residual): replaced the bootstrap-only proxy with a POSITIVE LAN-isolation witness. New shared policy daemon_libp2p::lan_isolation_or_refuse(LanReachability{bootstrap,provider_addrs,listen}) lives once next to LanShare (Protocol re-exported from fabric-libp2p); both thin binaries keep a lan_share_or_refuse(&Config) wrapper mapping the full config so the wiring is tested per binary. PUBLIC-REACH SIGNALS NOW CHECKED (refuse if any, naming TASK-103): (1) non-empty --libp2p-bootstrap; (2) non-empty --libp2p-provider-addr (THE residual: an external entry seeded into the kad routing table lets an empty-bootstrap provider still reach the public DHT); (3) --libp2p-listen not provably loopback/link-local (multiaddr_is_lan_only refuses public IPs, wildcard 0.0.0.0/::, and DNS names; fail-closed when no inspectable IP literal). Only no-bootstrap + no-provider-addr + loopback/link-local(or absent) listen mints LanShare. RELAY: not a shipped-config signal (no relay flag on the thin binaries); noted, nothing to check. BEFORE/AFTER: provider-addr+empty-bootstrap ANNOUNCED before -> REFUSES after; wildcard/public listen REFUSES; bootstrapped REFUSES; loopback-listen isolated provider PERMITTED. Bite verified by mutation at all 3 altitudes (shared policy + both binary wrappers): neuter provider_addrs check -> 3 RED; neuter multiaddr_is_lan_only -> listen tests RED. FIX B (defensive): added mutation-biting tests; NONE revealed a hole (cycle-1 crypto core holds), each lands as a regression guard. MAC: a_mac_reused_under_a_different_size (valid MAC over size 408 pasted on a line claiming 409 -> load fails LOUD; proves MAC binds BOTH fields, bites the MAC not the parser); a_positionally_swapped_record (column swap -> fails at typed parser); a_replayed_valid_line HONEST: exact replay does NOT fail loud (identical bytes => identical valid MAC) but CANNOT fabricate eligibility (a valid line only exists for an already-learned NAR; per-node secret key) -> dedups to one, never-learned stays ineligible. flip+foreign-key already existed; all MAC bites reddens under neutered MAC check. CORRELATION: a_narinfo_missing_url_does_not_learn (MissingField(URL) at learn); catalog::duplicate_field_skips_recording (parse_correlation first-occurrence+duplicate-reject, unified with public_allowlist::field; reddens when dup guard neutered); request-A-receive-B->RequestMismatch already covered. KEPT (cycle #1): sealed pub(crate) store, per-record keyed-blake3 MAC, consumed claim, exact-request correlation, 0600/nlink/bounds/flock, Nix-canonical fingerprint, integer NarSize, disabled-by-default. GATE (bounded, all green): cargo build; cargo test daemon-core+daemon+daemon-libp2p = 446 pass/0 fail; fmt --check; clippy --locked -D warnings; just independence; no_enumeration 11; doc_citations 3. GOTCHAS: (a) doc_citations flags any backticked snake_case with >=3 underscores in daemon/src/main.rs comments as a citation; lan_isolation_or_refuse lives in daemon-libp2p (not a scanned SOURCE) so the intra-doc link is written module-qualified ([daemon_libp2p::lan_isolation_or_refuse]) - the '::' fails the scanner's char filter, dodging the false-citation without pulling a new file into the gate. (b) CONSERVATIVE posture: a PRIVATE-LAN listen (192.168/10/172.16) is now REFUSED, not just public - only loopback/link-local is provably isolated; the isolated-LAN MVP that still runs is genuinely loopback/link-local. HONEST LIMIT: this only makes the isolation guard sound; real allowlist enforcement replacing it stays TASK-103's hard blocker (already filed).

DONE 2026-08-14. Public-NAR publication safety gate: crypto core (verify_strict + sig-covered NarHash/NarSize), sealed pub(crate) store (forged-store fails to compile), per-record keyed-blake3 MAC (corruption/foreign-key/flip rejected on load), consumed unforgeable ApprovedPublicProvision/PublicNarClaim, exact-request correlation, 0600/nlink/bounds/flock, Nix-canonical fingerprint. Codex DEEP gate (1 NO-GO + fixes) found real vulns the surface tests missed; fix cycle #1 closed 6 blockers, fix cycle #2 closed the residual provider-addr public-reach bypass with a POSITIVE LAN-isolation witness (refuses announce on ANY public-reach signal: bootstrap / provider-addr / non-loopback-link-local listen; fail-closed on no IP literal). Orchestrator-verified: provider-addr+empty-bootstrap now REFUSES (was announce); bootstrapped/public-listen refuse; isolated-loopback permitted; MAC size-sub/swap/foreign/flip reject; correlation missing-URL/dup/mismatch reject; 446 tests / 0 failed; independence+no_enumeration green. HONEST: MAC exact-replay is a documented no-op (can't fabricate eligibility - a valid line exists only for an already-learned NAR); private-LAN listen now refused (only loopback/link-local provably isolated - stricter, fail-closed). Allowlist ships DISABLED-by-default; real allowlist enforcement REPLACING the isolation guard is TASK-103's HARD BLOCKER (filed). Closed on orchestrator verification (not a 4th codex pass; 3 codex-waiter tooling misfires this gate, key completeness finding already fixed, categorical fix).
<!-- SECTION:NOTES:END -->
