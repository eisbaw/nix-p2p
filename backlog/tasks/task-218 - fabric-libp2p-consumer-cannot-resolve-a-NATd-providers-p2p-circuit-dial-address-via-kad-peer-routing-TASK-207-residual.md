---
id: TASK-218
title: >-
  fabric-libp2p: consumer cannot resolve a NAT'd provider's /p2p-circuit
  dial-address via kad peer-routing (TASK-207 residual)
status: Done
assignee: []
created_date: '2026-08-15 12:35'
updated_date: '2026-08-15 17:05'
labels:
  - libp2p
  - fabric
  - nat
  - hardening
dependencies: []
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Real-NAT VM harness (TASK-207, nixos/nat-vm-test.nix) proved: a NAT'd provider obtains a circuit-v2 relay reservation over a real NAT (ReservationReqAccepted), a direct dial to its private address is blocked (negative control), and the consumer DISCOVERS the provider record via kad get_providers. BUT the end-to-end NAR fetch does not complete: the consumer's node-locator gets a 'kad peer-routing miss' resolving the provider's /p2p-circuit dial-address, even though the provider self-advertises that address via --libp2p-external-address and holds a live reservation. Root area: fabric-libp2p/src/locator.rs locate_via_dht -> swarm.rs locate_peer/get_closest_peers + the identify->kad.add_address path (swarm.rs ~1150) for circuit multiaddrs. The fabric API-level proof (fabric-libp2p/tests/nat_traversal.rs) shows the relay DATA path is load-bearing when the circuit address is supplied directly (add_address+dial), so the gap is specifically the DHT-side propagation/resolution of a /p2p-circuit address to a consumer that only did kad discovery (no injection). Fixing this closes TASK-168 AC#1 (real-NAT relay fetch) and flips the RESIDUAL subtest in nat-vm-test.nix to a byte-identical relay-carried fetch.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A consumer that discovers a NAT'd provider via kad get_providers resolves the provider's /p2p-circuit dial-address via kad peer-routing (no --libp2p-provider-addr injection) and fetches a NAR byte-identical THROUGH the relay, proven by flipping the RESIDUAL subtest in nixos/nat-vm-test.nix to a positive relay-carried fetch
- [x] #2 The fix does not weaken the no-injection oracle (check-discovery-no-shortcut.py) nor the kad-exclusive discovery guarantee
- [x] #3 AC#1 resolution is scoped to the SINGLE shared-relay case (the provider reserved on a relay in the consumer bootstrap/relay config); the general multi-relay case is out of scope and filed as TASK-219, documented in the locator module doc, fabric-libp2p/tests/nat_dht_resolve.rs, and NodeConfig.known_relays
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
TASK-207 hardening (2026-08-15): 218 scope now INCLUDES delivering the LOAD-BEARING B2 reachability bite that 207 could only defer. nat-vm-test.nix ships SUPPORTING structural guards only (the real ReservationReqAccepted positive proof; zboot relay-server DISABLED so no alternative relay path exists; the B1 direct-path block; provider MainPID liveness across a relay stop; per-VM journal-cursor discipline). It does NOT prove the relay is load-bearing: a provider-side no-reservation-after-relay-stop bite is TAUTOLOGICAL because libp2p-relay 0.18 reservation renewal is connection-scoped, so a running provider silently drops its lease on relay disconnect and emits nothing gateable (measured: zero relay-client journal activity for 120s post-stop, even at libp2p_relay=debug). When 218 unblocks the /p2p-circuit byte-fetch, ADD the consumer-side load-bearing bite to nat-vm-test.nix: POSITIVE CONTROL = a FRESH consumer fetches the store path byte-identical THROUGH the relay; BITE = with the relay stopped, a FRESH unwarmed consumer dial/fetch of the same path FAILS within a bounded timeout, while provider MainPID is unchanged and the provider sole reservation relay is the relay. That fresh-dial-fails bite is the real B2 load-bearing proof.

TASK-218 spike/diagnosis DONE (fabric-libp2p/tests/nat_dht_resolve.rs, loopback R=relay+bootstrap / P=circuit-only-reachable / C=kad-discovery-only, NO injection). ROOT CAUSE: kad get_closest_peers for the NATd provider returns ONLY the providers DIRECT transport addr, never its /p2p-circuit addr. Probe: the relays OWN routing-table entry for P (returned via FIND_NODE to C) also carries only the direct addr - the circuit addr is dropped in the identify->kad->FIND_NODE path on the relay (libp2p 0.54 address handling). On loopback the direct addr is 127.0.0.1 so a fetch incidentally succeeds; in the real-NAT VM the direct addr is a PRIVATE unreachable addr and the circuit is the only reachable one -> kad peer-routing miss -> upstream fallback. Hypothesis (b)/(a) confirmed, NOT (c) timing. FIX direction: ROUTE 1 (construct circuit dial-address from discovered provider PeerId + a relay known from BOOTSTRAP CONFIG); consumer bootstrap in nat-vm-test.nix already includes the relay. mped-architect consult in flight for ROUTE1-vs-2 + seam. ROUTE 2 (DHT circuit-addr propagation, general multi-relay) to be filed as follow-up.

TASK-218 IMPLEMENTED (ROUTE 1, mped-architect GO with 5 must-fixes, all addressed). Fast gate GREEN; VM build pending.

FIX (ROUTE 1 - construct, no frozen-surface change): the PublicInfrastructure node-locator now returns a UNION of two INDEPENDENT dial-candidate provenances: (1) the kad peer-routing walk (unchanged honesty: own QueryReach classification + OurNodeId->DhtNode ledger record, no record on empty-routing short-circuit), and (2) a /p2p-circuit dial-address CONSTRUCTED as relayAddr/p2p/relayPeer/p2p-circuit/p2p/providerPeer from the provider PeerId (discovered via kad) plus a relay known from bootstrap config (NodeConfig.known_relays). Found iff union non-empty; else the DHT walk own honest Miss/Unavailable.

MUST-FIXES done: (1) union-of-two-provenances not a blended Found; (2) records Recipient::Relay->OurNodeId ONLY when circuit candidates added, separate from DhtNode, and added Relay to declared_exposure superset (frozen enum already has Relay - NO frozen-seam change); (3) known_relays is a config-level provider-INDEPENDENT Vec - proven by the provider-independence fabric bite (same relay set, two providers, distinct circuits) AND a NEW structural guard in check-discovery-no-shortcut.py that forbids known_relays being a provider/content-keyed map (self-tested: BTreeMap<NodeId,..> bites, Vec passes); (4) rewrote locate_via_dht + module doc so Found no longer claims learned-exclusively-through-DHT; (5) single-relay generality limit stated in locator doc, fabric test, NodeConfig.known_relays, AC#3, and filed as TASK-219.

SHOULD-filter: compose circuits only when kad returned no plausibly-PUBLIC addr (all loopback/RFC1918-private/link-local, or empty) - pure integer IP classification (ipv4_is_public/ipv6_is_public), NO floats.

FILES: fabric-libp2p/src/{locator.rs,swarm.rs,fabric.rs}, daemon-libp2p/src/lib.rs (bootstrap peers -> known_relays), fabric-libp2p/tests/nat_dht_resolve.rs (2 new tests, mutation-proven RED without composition), fabric-libp2p/tests/nar_transport.rs (locator ctor arg), scripts/check-discovery-no-shortcut.py, nixos/nat-vm-test.nix (residual flipped to gating AC#1 byte-fetch; SUPPORTING subtest rewritten to the B2 load-bearing consumer-side bite: fresh unwarmed consumer fetches byte-identical relay-UP, FAILS relay-DOWN, provider MainPID unchanged, sole reservation-relay is the stopped relay).

BITE PROVEN by mutation: neutering compose_circuit_locations -> both fabric tests go RED (test1: resolved addrs carry only the direct /ip4/127.0.0.1/tcp addr, no circuit; test2: Unavailable(InsufficientRouting)). Reverted.

GATE so far: cargo fmt --check OK; clippy -D warnings OK (fabric-libp2p+daemon-libp2p); cargo test fabric-libp2p+daemon-libp2p 156 passed / 0 failed; check-no-floats OK; check-discovery-no-shortcut self-test + scan OK (new provider-independence bite included); nix-instantiate --parse nat-vm-test.nix OK. VM build (nix build .#nat-vm-test) NOT yet run.

VM RUN 1 (build2 pending): AC#1 GATED PASSED in the real-NAT 6-VM harness — nodeb fetched payloadA BYTE-IDENTICAL THROUGH the relay circuit (kad-discovered PeerId + bootstrap-known relay construction). The fix works end-to-end behind a real NAT. B2 subtest FAILED on run 1 (positive control realise loop timed out, exit 124): GOTCHA — restarting the consumer daemon for a fresh-dial drops its in-memory narinfo->SignedNarHash correlation (daemon-core catalog), and nix REUSED its cached narinfo (skipping the daemon narinfo endpoint), so the NAR request fell to the URL-less UpstreamPath which the p2p source cannot serve — the fetch never even ATTEMPTED the circuit (log: "a p2p source cannot resolve an UpstreamPath (no URL)"). FIX: set nix.settings.narinfo-cache-positive-ttl=0 on the consumer so every realise re-requests the narinfo from the fresh daemon (re-correlating SignedNarHash); simplified make_nodeb_fresh (dropped the ineffective rm -rf /root/.cache/nix). The narinfo-upstream HTTP server runs on the relay VM as a SEPARATE service that SURVIVES stopping the relay nix-p2p-daemon, so the bite failure is purely the dead circuit + blocked direct path, not a narinfo outage. Re-running nat-vm-test (build2).

VM RUN 2 (build2): B2 positive-control UpstreamPath issue FIXED (narinfo-cache-positive-ttl=0 confirmed working — narinfo re-requested each realise). But run2 hit a TIMING FLAKE: kad bootstrap convergence through the two symmetric NATs took ~260s this run (vs 30s in run1), so the consumer OutboundCircuitEstablished at uptime 344s — just past the 300s DISCOVERY gate (exit 124). The fix is CORRECT (circuit established, byte-fetch would succeed); the gate budgets were too tight and the two racing 300s sub-gates were fragile. RUN 3 fix (harness robustness, not code): (1) AC#1 consolidated to ONE 600s realise-until-success loop + post-hoc discovery-observability assertion (removes the racing sub-gates); (2) B2 positive control bumped 300->600s; (3) B2 bite made HONEST via converge-then-fail — after the fresh restart it waits (cursor-scoped, 600s) for the fresh consumer to re-discover the provider via zboot DESPITE the relay being down, THEN asserts the fetch FAILS (timeout 90) — so the failure is attributable to the DEAD CIRCUIT, not un-converged routing. narinfo-upstream is a SEPARATE service on the relay VM that survives stopping the relay nix-p2p-daemon, so the bite failure is purely reachability. Re-running (build3).

VM RUN 3 (build3): AC#1 byte-fetch SUCCEEDED (circuit established at uptime 280s within the 600s budget) but the post-hoc discovery-observability grep FAILED — ROOT CAUSE: the "discovered N provider record(s)" string was ONLY emitted on the MISS path (daemon-core/src/peer_source.rs:207, "...but none yielded verified bytes"); a CLEAN fetch success logs no such line, so a success-without-prior-miss leaves discovery unobserved. FIX (code, generally useful observability, mped fail-verbosely): added an eprintln in peer_source.rs::resolve RIGHT AFTER find_providers returns Found (before any fetch) — "daemon: discovered N provider record(s) for <key> via kad" — so discovery is observable on BOTH success and miss, independent of RUST_LOG (stderr->journal like the existing p2p-miss eprintln). This also makes the B2 bite convergence-wait reliable (it keys off the same line). daemon-core 179+ tests green, fmt/clippy clean. NOTE: kad bootstrap convergence through the two symmetric NATs is genuinely SLOW+variable (~280s in run3), hence the 600s budgets. Re-running (build4).

VM RUN 4 (build4): AC#1 GATED PASSED again (discovery log fired, byte-identical fetch through relay). B2 positive control (fresh restarted consumer) FAILED — repeatedly established circuits but the realise never completed. ROOT CAUSE (pre-existing daemon quirk, NOT TASK-218): the test narinfo LACKED FileHash/FileSize. When a peer is discoverable (will_serve_raw=true), the daemon attempts rewrite-to-raw (daemon-core server.rs:359-386 / rewrite.rs), which REQUIRES FileHash; missing it, the rewrite is skipped and the narinfo is served VERBATIM recording NO token->SignedNarHash correlation, so the follow-up NAR request falls to the URL-less UpstreamPath and p2p is never attempted. AC#1 only passed by hitting the will_serve_raw=false window (correlation recorded on the verbatim else-branch). The fresh-restart B2 consumer hit the bad branch persistently. FIX (test fixture, root cause): sign-narinfo.py now emits FileHash=NarHash and FileSize=NarSize (correct for Compression: none; UNSIGNED so the Sig is unchanged; matches what a real cache writes and what rewrite.rs documents lines 34-62). This makes the token->SignedNarHash correlation DETERMINISTIC (both rewrite and verbatim branches record it), so discovery+relay-fetch is reliable across a fresh restart. Kept narinfo-cache-positive-ttl=0 (still needed so a fresh daemon re-correlates). Re-running (build5); daemon binary unchanged so mostly cached.

VM RUN 5 (build5): FileHash fix WORKED — AC#1 GATED and B2 POSITIVE CONTROL both passed (fresh consumer fetched byte-identical relay-UP; no more UpstreamPath, misses now show the healthy "directory unavailable: consultation deadline exceeded" pre-convergence state). But the B2 BITE (fresh RESTARTED consumer, relay DOWN, converge-via-zboot-then-fail) hung: the fresh consumer never re-discovered the provider via zboot alone (relay down) within 600s — kad re-convergence via a SINGLE surviving bootstrap behind the NAT is slow+unreliable, and requiring it confounds a REACHABILITY bite with a DISCOVERY one. REDESIGN (run6): the B2 bite now uses the SAME AC#1-converged consumer (no restart, no re-discovery). POSITIVE CONTROL: delete path + re-fetch relay-UP -> byte-identical (routing already warm). BITE: (a) assert NO DCUtR direct upgrade ever occurred (grep "dcutr hole-punch upgraded a relayed connection to DIRECT" is ABSENT — so the consumer has no hole-punched direct connection that could survive the relay stop); (b) stop the relay (severs the circuit-v2 connection, which is forwarded THROUGH the relay), confirm down from both nodea+nodeb, settle; (c) delete path + re-fetch -> MUST FAIL (dead relay circuit + NAT-blocked direct, B1); provider MainPID unchanged. This is honest (the DCUtR-absence guard + the connection-severing logic prove the re-fetch has NO reachable path) and sidesteps the re-discovery flake. Re-running (build6, mostly cached).

VM RUN 6 (build6): PASSED — BUILD_EXIT=0. Full real-NAT 6-VM harness green: (1) AC#1 GATED: nodeb fetched payloadA BYTE-IDENTICAL THROUGH the relay circuit behind a real NAT (kad-discovered PeerId + bootstrap-known-relay construction, no injection); discovery observable via the new "discovered N provider record(s) ... via kad" log. (2) B2 POSITIVE CONTROL: warm consumer re-fetched byte-identical (relay UP). (3) B2 LOAD-BEARING PROVEN: with the relay stopped, the re-fetch FAILED in 50s (within the 120s bound), NO DCUtR direct upgrade ever occurred, provider MainPID 709 unchanged, sole reservation relay was the stopped relay. The relay circuit carries the NAR bytes - load-bearing, not incidental. AC#1 and AC#2 both MET; the TASK-207-deferred B2 load-bearing consumer-side reachability bite is DELIVERED. Fast gate green: cargo test 460 passed/0 failed (fabric-libp2p+daemon-libp2p+daemon-core+peer-fabric); fmt/clippy -D-warnings/no-floats/discovery-oracle(self-test+scan) clean. Frozen surfaces untouched. Running final qa-test-runner + mped-architect before commit.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
ROUTE 1 landed (mped-architect GO, all 5 must-fixes addressed; final review GO). A discovery-only consumer resolves a NAT'd provider's /p2p-circuit dial-address by CONSTRUCTING it from the kad-discovered provider PeerId + a relay known from bootstrap config, and fetches the NAR byte-identical THROUGH the relay - no injection, frozen surfaces untouched. locate_via_dht is a UNION of two provenances (kad walk with unchanged QueryReach/DhtNode-ledger honesty + relay-circuit construction recording Recipient::Relay only when candidates are added), composed only when kad returned no plausibly-public addr (integer IP classification, no floats). NodeConfig.known_relays (provider-independent Vec) is fed from bootstrap config. AC#1 (byte-identical relay-carried fetch) and AC#2 (no-injection oracle + kad-exclusive discovery NOT weakened; a NEW self-tested guard forbids a provider-keyed known_relays map) both MET; AC#3 documents the single-relay generality limit (multi-relay = TASK-219). The TASK-207-deferred B2 LOAD-BEARING consumer-side reachability bite is DELIVERED in nat-vm-test.nix (same converged consumer: relay-UP re-fetch byte-identical vs relay-DOWN re-fetch FAILS, DCUtR-absence guard, sole-relay, provider MainPID unchanged). PROOF: nix build .#nat-vm-test PASSED behind a real NAT (BUILD_EXIT=0); fabric tests mutation-proven (RED without circuit composition) + provider-independence bite; 460 unit tests pass; fmt/clippy -D-warnings/no-floats/discovery-oracle green. Follow-ups filed: TASK-219 (general multi-relay via DHT propagation), TASK-220 (daemon over-rejects a Compression:none narinfo lacking FileHash, silently disabling p2p; worked around in the harness fixture).
<!-- SECTION:FINAL_SUMMARY:END -->
