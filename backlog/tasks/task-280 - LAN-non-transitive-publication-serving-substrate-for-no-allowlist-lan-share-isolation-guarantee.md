---
id: TASK-280
title: >-
  LAN-non-transitive publication + serving substrate for no-allowlist lan-share
  (isolation guarantee)
status: In Progress
assignee: []
created_date: '2026-08-20 07:29'
updated_date: '2026-08-20 13:56'
labels:
  - irreversible
  - privacy
  - security
dependencies:
  - TASK-276
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Splits out the CRITICAL egress-transitivity defect codex found during TASK-276 (Mark-emulator: listener-INDEPENDENT, so PRE-EXISTING, fires identically under the pre-276 loopback default; 276 did not create it). A lan-share node feeds mDNS/Kad-returned/Identify addresses into Kademlia UNFILTERED, dials them, announces transitively (start_providing/put_record), and serves /nar over ANY established connection incl provider-ORIGINATED (libp2p bidirectional) -> a dual-homed same-v1 Kad node on the LAN also joined to public peers bridges the provider to the public DHT; a public peer fetches over the outbound connection, never traversing the private listener. Cannot fire by default (no default public swarm at HEAD) but falsifies the lan-share public-isolation GUARANTEE. This task holds that guarantee.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 No-allowlist lan-share path filters mDNS / Kad-returned / Identify addresses to LAN-only literals (reuse the TASK-276 FIX#1 positive-grammar classifier) BEFORE they enter Kademlia or the dial queue -> the node never dials a non-LAN peer address
- [x] #2 /nar serving is restricted to connections of LAN PROVENANCE (peer observed remote address is loopback/link-local/private) -> an outbound connection to a non-LAN peer cannot be used to serve (closes the bidirectional-serve leg)
- [x] #3 Evaluate a DISTINCT Kademlia protocol scope for lan-share (not the shared v1) so a lan-share node's DHT is structurally not the public DHT; decide in-task whether address-filtering + serve-provenance suffice or the scope split is required
- [x] #4 Biting e2e (the codex exploit as a negative control): a dual-homed same-v1 bridge node on the LAN + a public peer; the public peer CANNOT learn the content key and CANNOT fetch. RED against today's HEAD (proves it bites), GREEN after the fix
- [x] #5 Consumer/provider scope PARITY: the zero-config lan-share CONSUMER defaults to the SAME new lan-share scope as providers (or discovery silently breaks). --libp2p-scope still overrides (advanced escape hatch); public-share stays on v1
- [x] #6 WIRE FREEZE: the lan-share scope string is a compatibility surface -> use a VERSIONED constant (lan-share.v1) and FREEZE it in PRD.md (correct risk #13); pick now while TASK-276 cross-host serving is unreleased (no deployed lan-share-on-v1 base to strand)
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
RE-SPEC (Mark-emulator after codex NO-GO + live e2e regression). Must-fix ALL 7:
#1 EXACT positive grammar for multiaddr_lan_provenance (lan.rs:78): structural whitelist over addr.iter() -> accept ONLY <lan-ip>(Ip4/Ip6 lan-literal) + <direct-transport>(Tcp | Udp+QuicV1) + optional terminal /p2p/<id>; REJECT any 2nd Ip4/Ip6, P2pCircuit, Dns*, Ws/Wss/Tls/Sni, unknown, or post-/p2p trailing. Feeds BOTH dial veto + serve provenance. Test the compound-addr bypass rejects.
#2 PER-CONNECTION serve auth (DECIDED: modify vendored libp2p-stream, we own it): thread ConnectionId through IncomingStreams (control.rs/behaviour.rs) -> Item=(PeerId,ConnectionId,Stream); ledger keys (PeerId,ConnectionId); run_accept_loop gates on THAT exact conn_id in the LAN-provenance set (not "peer has any LAN conn"). Mutation: serve on a different/unrecorded conn_id refused.
#3 SCOPE=AUDIENCE model (symmetric): lan-share = serve+consume on lan-share.v1; a LAN leech uses --libp2p-scope lan-share.v1; public-share/consume-only default v1. De-vacuum parity tests (real LanShare provider + real ConsumeOnly+explicit-scope agree; bare ConsumeOnly = v1). LOUD WARNING when default v1 + consume-capable + reach is only mdns/bootstrap: "on public v1; a LAN lan-share cache uses lan-share.v1 -- pass --libp2p-scope lan-share.v1 or --profile lan-share". Fix the zeroconfig e2e topology (all pool nodes lan-share.v1).
#4 IDENTIFY not wire-scoped: correct the "kad+identify+nar all namespaced" claim (lan.rs module doc, PRD:563) -- only kad+nar negotiate scoped substreams; identify negotiates /ipfs/id/1.0.0 with scope in protocol_version METADATA. HARDENING: on the identify receive path under confinement (swarm.rs:2468) reject a peer whose info.protocol_version != /nix-p2p/<scope>/id/1.0.0 (drop its addresses). Freezes the identify protocol_version string.
#5 POST-NOISE leak: correct lan.rs:127 + PRD:556 (a non-LAN dial completes a Noise session -- peer-id+src-addr exposed -- before rejection, but NO kad/identify/nar app substream, so no record/membership/byte). ROOT-CAUSE: TRANSPORT-LEVEL pre-connect dial filter (pre-Noise; covers kad autonomous by-PeerId dials; applies #1 grammar) -- BOUNDED SPIKE first vs pinned libp2p 0.56 SwarmBuilder+with_relay_client; fallback = keep behaviour veto + reorder lan_dial_guard FIRST (swarm.rs:311) + disclose residual. Reorder first regardless (free).
#6 SINGLE-SOURCE: derive lan_share ONCE from provider_publication_decision (the PublicationPlan::Lan mint); thread to both network_scope + lan_confinement (kill the two raw recomputations). Disclosure prints the EFFECTIVE scope (not hardcoded lan-share.v1). PRD:539 add the insider residual.
#7/AC#4 (BLOCKING): augmented adversarial e2e -- (a) compound-addr: assert NO TCP SYN/Noise to <PUBLIC> (pcap on public net); (b) connection-confusion: serve on non-LAN conn_id refused; (c) cross-scope identify: v1 peer identify -> addresses NOT ingested/dialed; (d) pre-Noise observation (documents #5 residual or GREEN if transport filter lands); RETAIN the v1-bridge-cannot-learn-K-nor-fetch oracle (RED-at-HEAD via query-terminal wait). Topology: pool nodes lan-share.v1, adversary v1.
WIRE FREEZE: lan-share.v1 still pick-now; frozen surface = kad + nar + (now) identify protocol_version strings; the #1 grammar + #2 conn-auth are LOCAL admission (NOT frozen). PRD #13 enumerate the frozen strings.
Gate: full DEEP qa+mped+codex + augmented AC#4.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
TASK-280 implementation (commits 5e44a2e fabric, c0618fa daemon layer). AC#1/#2/#3/#5/#6 DONE + mutation-proven (RED-on-revert then reverted). AC#4 (biting e2e) OPEN: feasibility CONFIRMED (rootless dual-homed 2-network podman verified: eth0 LAN 10.211.99.5 + eth1 non-private 203.0.113.5), NOT yet built/run.

Design: three composed mitigations gated on the no-allowlist lan-share path only (NodeConfig.lan_confinement, set iff profile==LanShare / PublicationPlan::Lan).
(1) DIAL VETO: LanDialGuard NetworkBehaviour. Chokepoint is handle_established_outbound_connection (per concrete address), NOT the pending hook — verified against shipped sources that libp2p-kad 0.48 dials by PeerId with empty DialOpts and supplies addresses via its OWN pending-hook return, and the derive passes every sibling field the same empty slice. Pending hook additionally denies explicit all-non-LAN dials pre-transport; add_address gated as hygiene. Honest residual: a transport connect may briefly occur before deny, but no usable libp2p session forms.
(2) SERVE PROVENANCE: worker records each kept connection LAN provenance (remote=dialed addr for outbound / send_back_addr for inbound; relayed=non-LAN) into a shared live view; run_accept_loop refuses serving a peer with no live LAN-provenance connection (fail-closed).
(3) DISTINCT SCOPE (WIRE FREEZE): LAN_SHARE_NETWORK_SCOPE=lan-share.v1 single-sourced in fabric-libp2p; effective_network_scope(explicit,lan_share) selects it; both binaries agree; consumer/provider parity is structural (one fabric, one scope). PRD risk #13 records the freeze.

Shared classifier moved DOWN to fabric-libp2p/src/lan.rs (ip_is_provably_private re-exported by daemon-libp2p; new multiaddr_lan_provenance).

AC#4 e2e SPEC (harness-verified by explore): new topology in scripts/e2e_harness.py modeled on Libp2pLanShareServeTopology (:2765); TWO podman networks LAN + a genuinely NON-private public net (use 203.0.113.0/24 TEST-NET-3, NOT 10.99.0.0/24 which is RFC1918 and would classify LAN); P=--profile lan-share on LAN only seeds K; X dual-homed (--network lan --network pub) standard /bin/daemon on scope v1 bootstrapping P_pub; P_pub on public net only runs get_providers(K)+/nar fetch. Oracles reuse existing: proxy upstream.nar>=1 + absence of daemon-core/src/peer_source.rs:194 discovered-provider-record log line within a bounded poll of P_pub query terminal (NOT a sleep); attribute DIAL leg (P never dials 203.0.113.x) + SERVE leg (public-provenance /nar refused). Register in SCENARIOS + E2E_FAST. RED-at-HEAD must be proven against the reverted mitigations, positively observing P_pub learning K at pre-fix HEAD.

Gates green: cargo test -p daemon-core -p daemon-libp2p -p daemon -p fabric-libp2p (exit 0); cargo fmt --all --check; ruff check scripts; nix-instantiate --parse nixos/nix-p2p.nix. NOT marking Done (AC#4 open).

codex DEEP security review VERDICT_NO_GO - composition does NOT hold; concrete bypasses source-found (per-boundary unit proofs + a normal bridge e2e would MISS these). #1 CRIT: multiaddr_lan_provenance checks FIRST ip hop but libp2p TCP dials the TERMINAL pair -> /ip4/<lan>/tcp/1/ip4/<PUBLIC>/tcp/4001/p2p/<id> classified LAN, connects PUBLIC -> bypasses BOTH dial veto + serve provenance; learnable via unscoped Identify. FIX: exact-grammar (one LAN ip + tcp | ip/udp/quic-v1, <=1 terminal peer-id; reject extra addr-bearing comps). #2 CRIT: /nar auth per-PEER not per-CONNECTION (ledger keys peer; lan_serve_permitted = any LAN conn; libp2p-stream control.rs:141 drops ConnectionId) -> peer with 1 LAN conn serves over a 2nd non-LAN conn. FIX: preserve ConnectionId OR reject non-LAN inbound before stream-accept. #4 HIGH: Identify NOT wire-scoped (swarm.rs:3534 -> identify::Config protocol_version is METADATA only; negotiated proto stays /ipfs/id/1.0.0; receive path does not reject mismatched protocol_version) -> enables #1 cross-scope addr exchange; the kad+identify+nar-all-namespaced claim + PRD:563 is FALSE. #3 HIGH: consumer scope parity broken (bare --libp2p-mdns -> ConsumeOnly -> v1; provider lan-share.v1 -> silent no-discovery); parity test vacuous. #5 HIGH: established-hook veto fires AFTER transport upgrade + Noise auth (peer id + src addr leak; lan.rs:127/PRD:556 "before Noise/no session" inaccurate); guard should be FIRST in behavior order. #6 MED: confinement recomputed from raw not selected once from PublicationPlan; disclosure says lan-share.v1 under override; PRD:539 omits insider residual. #7: e2e:7162 pins helpers v1 while zero-config node now lan-share.v1 -> topology inconsistent. Routing fix spec to Mark-emulator.

REGRESSION CONFIRMED (full e2e): libp2p-lan-share-zeroconfig FAILED 13/14 - node B (--profile lan-share, now scope lan-share.v1) fell back to upstream (upstream.nar=1) because the scope split isolated it from the v1 provider/helpers in the topology. So codex #3 / mped F2 (consumer/provider scope parity) is LIVE gate-breaking, not latent: the scope split breaks 273 own zero-config scenario. Other 13 scenarios (incl cross-host-serve 12/12) pass. The scope model must ensure a same-pin lan-share POOL agrees on lan-share.v1 (audience independent from role) AND the zeroconfig e2e topology must put all pool nodes on the same scope. Mark-emulator speccing.

CODEX-NOGO FIXES delivered (commits 8a4a088 vendor-ConnectionId, b5c0ecd fabric-hardening, 779305f daemon-scope/single-source, c9ef426 PRD+e2e). All 7 re-spec items addressed; AC#4 (biting system e2e) still OPEN.

#1 EXACT positive grammar (lan.rs multiaddr_lan_provenance): structural whitelist over the WHOLE sequence (one LAN ip + Tcp|Udp+QuicV1 + optional terminal p2p); compound-address bypass rejected. MUTATION-PROVEN RED-on-revert: provenance_rejects_compound_address_bypass (i==comps.len -> true).
#2 PER-CONNECTION serve gate: ConnectionId threaded through vendored libp2p-stream (IncomingStreams item now (PeerId,ConnectionId,Stream)); lan_serve_permitted keys (PeerId,ConnectionId). MUTATION-PROVEN: lan_serve_gate_refuses_a_non_lan_connection_of_a_lan_peer (contains -> !is_empty).
#3 SCOPE=AUDIENCE: parity tests de-vacuumed (real LanShare provider + real ConsumeOnly opt-in agree; bare ConsumeOnly=v1) via source_config; startup HINT should_hint_lan_share_scope wired into both binaries. MUTATION-PROVEN: effective_scope_is_audience_across_roles (explicit no longer wins).
#4 IDENTIFY: corrected the all-three-scoped claim (lan.rs doc + PRD:563); receive-gate rejects cross-scope protocol_version under confinement (swarm.rs). MUTATION-PROVEN: identify_gate_rejects_cross_scope_protocol_version_under_confinement (advertised==expected -> true). identify protocol_version now a frozen wire string.
#5 POST-NOISE honesty corrected (lan.rs + PRD:556): a non-LAN dial completes a Noise session before the deny; NO app substream. SPIKE OUTCOME: transport-level pre-connect filter NOT feasible vs pinned libp2p 0.56 phased SwarmBuilder (no transport-wrap hook; with_other_transport only OR-appends; T is pub(crate)). FALLBACK taken: LanDialGuard reordered FIRST in the Behaviour derive; residual disclosed.
#6 SINGLE-SOURCE: composite daemon derives lan_share ONCE from PublicationPlan::Lan and threads it + the plan into install_libp2p_provider; the raw recomputation is gone; disclosure prints the EFFECTIVE scope. PRD:539 insider residual added.

REGRESSION FIXED: libp2p-lan-share-zeroconfig topology moved the whole pool (A+C) onto lan-share.v1 to match B (profile-derived).

GATES: cargo test -p fabric-libp2p -p daemon-libp2p -p daemon -p daemon-core -p libp2p-stream = all green (one load-flaky iroh_daemon_shutdown startup-timeout, passes isolated). cargo fmt --all --check green; ruff check scripts green; nix-instantiate --parse nixos/nix-p2p.nix green. 4/4 key mutations proven RED-on-revert then reverted.

AC#4 STILL OPEN: unit-level bites for all four findings delivered + mutation-proven, but the full two-network podman adversarial system e2e (LAN 10.211.34.0/24 + PUBLIC 203.0.113.0/24 TEST-NET-3; P lan-share, X dual-homed v1 bridge, P_pub public leech; pcap/log oracle + per-mitigation RED-at-HEAD revert) is a multi-cycle build, deferred with the precise plan already in the notes above. Do NOT register in E2E_FAST until it passes RED-at-HEAD then GREEN.

codex fix-review VERDICT_NO_GO (code-level, indep of AC#4). VERIFIED FIXED: #1 exact grammar (compound/relay/dns/global rejected), #2 per-connection serve auth (ConnectionId exact+unspoofable via vendored path), scope-as-audience real+non-vacuous, KAD+NAR scoped + guard-first. STILL OPEN: (BLOCKER) #4 identify address-ingestion NOT closed - the receive-gate only suppresses app kad.add_address, but libp2p-identify 0.47 internally caches advertised listen addrs (cache=100) before the event -> global addr stays a dial candidate for later kad by-PeerId dial (completes Noise/TLS before LanDialGuard denies; DialError::Denied does not evict). FIX: construct Identify with_cache_size(0). (TEST-QUALITY) unit mutations do NOT bite production: serve tests (swarm.rs:4507) manually populate the ledger + call a helper (removing the accept-loop check at 2859 stays green); identify test (4555) is string-equality only. HIGH: composite scope-warning passes raw libp2p_leech not derived ConsumeOnly (daemon:2735) so daemon --libp2p-mdns stays silent-on-v1; the hint wrongly suggests --profile lan-share for a consumer (remedy is explicit scope). MED: PublicationPlan not single-source in thin binary (derives from SharingProfile at main.rs:962 then plan later at 1241; equivalent today, can drift); operator.rs:111 disclosure claims lan-share.v1 even under --libp2p-scope v1 override. #5: QUIC uses TLS not Noise so "one dropped Noise session" mis-bounds the residual; codex DISPUTES transport-filter infeasibility (an enclosing NetworkBehaviour can delegate pending resolution + filter the combined kad/identify address result). PAUSED per owner "stop all rust compile".

RE-SCOPE (COMPASS + owner compile-freeze): 280 narrows to 280-CORE = the concrete known leaks + the AC#4 biting e2e (the guarantee). Transport-level pre-connect filter split to TASK-281 (LOW, disclosed residual, over-investment vs the org/LAN-first threat model). 280-CORE must-do (when compile resumes): (1) construct Identify with_cache_size(0) so cross-scope advertised addresses are not cached as dial candidates (the identify receive-gate alone cannot undo the internal cache); (2) composite scope-warning: pass the DERIVED ConsumeOnly, not raw libp2p_leech (daemon:2735), + fix the hint to recommend --libp2p-scope lan-share.v1 (not --profile lan-share) for a consumer; (3) single-source scope+confinement from PublicationPlan in the thin binary (daemon-libp2p:962 vs 1241) + fix operator.rs:111 disclosure under --libp2p-scope override; (4) make the serve/identify unit mutations bite the PRODUCTION path (current tests manually populate the ledger/helper -> deleting the accept-loop check stays green = decoration); (5) AC#4 two-network adversarial e2e proven RED-at-HEAD -> GREEN (the load-bearing artifact). AC#4 e2e HARNESS can be authored in Python during the freeze (only RUNNING needs compile).

AC#4 e2e AUTHORED (Python-only, compile-frozen; commit 2d12025). scripts/e2e_harness.py: new scenario_libp2p_lan_share_isolation_bridge + Libp2pIsolationBridgeTopology (dual-homed-bridge negative control). TWO podman nets: LAN 10.211.34.0/24 (RFC1918) + PUBLIC 203.0.113.0/24 (TEST-NET-3). Roles: P --profile lan-share (lan-share.v1) seeds K LAN-only; H same-scope LAN quorum peer + positive control; X dual-homed standard v1 bridge P meets over mDNS (fixed BOOT identity so P_pub bootstraps with an offline PeerId); P_pub PUBLIC v1 leech bootstrapped to X. New consts LIBP2P_LAN_SHARE_SCOPE=lan-share.v1, LIBP2P_DEFAULT_SCOPE=v1. Registered in SCENARIOS (runnable via --only libp2p-lan-share-isolation-bridge); DELIBERATELY NOT in Justfile E2E_FAST until validated (prior gate broke on an unvalidated fast scenario).

Oracles (each attributes to one mitigation; revert reddens THAT assert): (1) KEY leg=AC#3 scope split: P_pub get_providers(K) empty, waits on query TERMINAL via client_run completion (peer path resolves Found/Miss BEFORE upstream fallback, per peer_source.rs), re-derives from raw log absence of "provider record(s)"; revert=P+H back on v1. (2) DIAL leg=FIX#1 grammar+guard-first veto: no 203.0.113.x SYN (NET_RAW tcpdump sidecar sharing P netns) + no 203.0.113 dial line in P log; revert=relax multiaddr_lan_provenance. (3) SERVE leg=FIX#2 per-connection provenance: DOCUMENTED non-biting gate (unit-proven; NOT system-forcible here - P is LAN-only-listen, no route from P_pub). (4) cross-scope identify=FIX#4 cache_size(0)+protocol_version gate: shares the no-public-dial consequence, TODO to strengthen to a dedicated receive-gate/kad-absence observable. (5) END-TO-END: P_pub gets K from PUBLIC upstream (pub-proxy upstream.nar>=1), never peer-served from P; revert scope split -> ==0.

HONESTY: NOT run, NOT validated (owner compile-freeze). py_compile GREEN; ruff check GREEN (ruff format --check skipped: store ruff 0.15.14 reformats the WHOLE pre-existing file = version artifact; task required ruff check only; new code follows the file compact hand-format). No Rust compiled; scenario not run; NO AC marked. TODO(TASK-280 validate) markers left inline for: two-net podman join + name:ip= pinning, wildcard-listen identify advertising X public addr, pcap non-vacuity (single-homed P needs a default route to emit the SYN; empty capture only load-bearing after a reverted-HEAD SYN is observed), exact leech Miss terminal token, dial-vs-filter log token, identify-completed non-vacuity, convergence window. Each oracle RED-at-HEAD must be demonstrated by reverting its specific mitigation when compile resumes.

CODEX FIX-REVIEW RESIDUALS + AC#4 VALIDATION DELIVERED (280-CORE). Commits a729404 (fabric #1/#4), 4e191f5 (daemon #2/#3), 021680f (AC#4 e2e + E2E_FAST).

#1 identify address-cache bypass (BLOCKER): under lan_confinement, Identify is now built with with_cache_size(0) (PeerCache::disabled) so libp2p-identify 0.47 never internally caches a cross-scope peer advertised listen addr as a dial candidate; the receive-gate is kept as defence in depth. Off the lan-share path the library default is retained.

#4 production-biting mutations: serve gate refactored into accept_loop_core (the loop body run_accept_loop delegates to), driven by a unit test with sentinel items so deleting the accept-loop guard reddens it (PROVEN RED-on-revert, restored). identify gate extracted into identify_admitted_addresses (the COMPLETE gate the handler applies verbatim); a test drives it so dropping the LAN filter or admitting cross-scope reddens it (PROVEN RED-on-revert, restored). The prior tests called the pure predicate in isolation and stayed GREEN under the production-path deletion.

#2 composite scope-warning: now passes the DERIVED consume capability (contract.profile == ConsumeOnly), not raw libp2p_leech, so a bare daemon --libp2p-mdns (which derives ConsumeOnly without the leech flag) is now warned. The shared LAN_SHARE_SCOPE_HINT no longer suggests --profile lan-share for a consumer (that makes it a PROVIDER); it recommends --libp2p-scope lan-share.v1.

#3 single-source thin binary: source_config now takes lan_share as a caller-decided param; the provider path mints the PublicationPlan ONCE and threads matches!(plan, PublicationPlan::Lan(_)) into both network_scope and lan_confinement (mirroring the composite daemon), and install_provider takes the already-taken plan. Non-provider path passes false. Also fixed a latent substring-collision in the effective-scope override disclosure (the honest NOT-separate text contained separate-from-the-public and tripped the mutation-catch assertion; reworded to SHARED WITH the public DHT). The disclosure already printed the effective scope (respects --libp2p-scope override).

AC#4 VALIDATED (rootless podman, 2026-08-20): isolation-bridge scenario RUN, 11/11 GREEN at HEAD in ~72s. Topology stands up: rootless two-net join (--network net:ip=), dual-homed v1 bridge, wildcard-listen bridge, provider announce with H put-quorum, P_pub public-leg bootstrap. CORE GUARANTEE proven to BITE: reverting ONLY the AC#3 scope split (P+H to v1) reddens EXACTLY the KEY leg (P_pub log POSITIVELY shows discovered 1 provider record(s) via kad -- the codex leak) and END-TO-END (pub upstream.nar==0, P_pub peer-fetched K from P via the bridge); DIAL/identify stay GREEN (dial veto is profile-derived, scope-independent) = precise attribution. Restored -> GREEN. Registered in E2E_FAST.

LIMITATIONS (honest): DIAL + identify oracles pass GREEN at HEAD and their mitigations are UNIT-mutation-proven, but a clean SYSTEM single-mitigation RED for them is entangled by the layered defenses (relaxing FIX#1 grammar alone still leaves identify cache_size(0) suppressing the candidate, and vice-versa) and needs a multi-mitigation revert + full image rebuild -- left DOCUMENTED-pending, not faked. SERVE leg stays a documented unit-level bite (LAN-only-listen provider cannot be forced a non-LAN-provenance conn from this topology). pcap-SYN and identify-completed non-vacuity remain documented-open. E2E_FAST registration adds a rootless two-net-podman + NET_RAW dependency to just e2e.

GATES: cargo test -p daemon-core -p daemon-libp2p -p daemon -p fabric-libp2p -p libp2p-stream = 929 passed, 0 failed; cargo fmt --all --check green; ruff check scripts green; nix-instantiate --parse nixos/nix-p2p.nix green. NOT marking Done (orchestrator owns Done after the qa+mped+codex full-e2e re-gate).
<!-- SECTION:NOTES:END -->
