---
id: TASK-198
title: Re-state wan_shaped speedup with BOTH link ends shaped (task-70 AC#3)
status: Done
assignee: []
created_date: '2026-08-13 21:02'
updated_date: '2026-08-15 11:16'
labels:
  - measurement
  - transport
  - finding
dependencies:
  - TASK-70
  - TASK-99
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
DEFERRED FROM TASK-70 AC#3. The shaped-link measurement primitive landed in task-70 (scripts/shaped_link*.py + shaped_link_inner.sh): it emulates a real peer link (nested netns + veth + tc netem), asserts the shaping host-side with a negative control, and its oracle bites by mutation (--self-test). What is NOT done, and is deferred here: re-state the wan_shaped speedup with the PEER link ALSO shaped (currently only the upstream arm is shaped, in scripts/profile_p2p.py; the peer transport still runs over pod loopback). WHY BLOCKED ON TASK-99: task-70's own WIRE-COST CORRECTION forbids re-deriving any peer-vs-upstream speedup until link compression (task-99) lands, because peers serve RAW nar (~3.6x the bytes the xz CDN serves) and the peer byte-volume — hence the shaped-link speedup — depends on whether the link is compressed. Measuring now would produce a number task-99 invalidates. SCOPE: wire the real libp2p two-node NAR transfer THROUGH the shaped-link primitive (not the raw-TCP probe used to validate the primitive), run it under the same FROZEN counting rule profile_p2p uses, and state which side is the upper bound. Note the staleness: profile_p2p.py + daemon/examples/iroh_throughput.rs are iroh-worded; the shipped primary transport is now libp2p-stream.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
DEPRIORITIZED 2026-08-14 (owner steer): 'dont worry much about compression, ok to measure but dont lean into it — we want basics first like discovery and good robust connectivity. then we will consider CA chunking and compression later.' TASK-99 already MEASURED the compression thesis (near-parity on home uplinks, peers don't beat CDN). This task is a compression follow-on (speedup-restatement / pipelining / iroh-codec) — defer until the discovery + robust-connectivity trunk (TASK-103/151/191/194) is solid. Not next.

OWNER DECISION (2026-08-15): 'demonstrate it — do 198 next'. Compression-earlier means DEMONSTRATE, not just enable. 198 is the LIVE two-ends-shaped measured number that TASK-203's AC#3 conditional model defers to. Raised Low->High. Now cheap: TASK-206 built the shaped two-node libp2p harness (fabric-libp2p/examples/shaped_probe.rs + scripts/shaped_libp2p.py) and TASK-203 built the streaming serve — 198 = run the real streamed libp2p transfer through the existing shaped instrument, report measured speedup (integer-ns, integer-bytes/sec, exact-rational), negative-control shaping oracle (206 pattern). FOLD IN TASK-216 (flush-size sweep) as the same instrument, not a 3rd orbit. Sequence: after TASK-203 lands. Watch the NarSize-vs-compressed-bytes unit trap (recurred 3x).

IMPLEMENTED 2026-08-15 (5124e18 instrument, 0945439 evidence). Live two-ends-shaped raw-vs-zstd peer transfer — the measured counterpart TASK-203 deferred to. fabric-libp2p: added SwarmHandle::fetch_nar_streaming_measured(offer_zstd)->StreamedFetch{bytes,wire_body_bytes} (raw-only accept set forces raw; CountingReader tallies compressed body; shipped fetch delegates, no wire-framing change). shaped_probe example extended additively (compressible payload + raw|both codec arg + wire bytes; TASK-206 callers unaffected). scripts/shaped_compress{.py,_inner.sh}: runner+nested-netns driver, integer/exact-rational only, reuses shaped_link.assert_shaping (RAW arm shaped vs unshaped control), --self-test bites parse+verdict+shaping mutations; added to check-no-floats SCANNED. MEASURED (16 MiB compressible nar, 20ms->~48ms RTT, 20 mbit home uplink, 3 runs, BOTH ends shaped): wire ratio 16777216/4219763 ~3.976x; wall-clock speedup ~3.196x every run (raw ~7.66s, zstd ~2.40s); speedup sits below wire ratio by the shared per-fetch fixed cost (dial+handshake+RTT ramp), stated. SIGN robust: min margin 5.25e9 ns dwarfs ~17e6 ns spread (~300x) — bandwidth-bound wire-byte diff, not a scheduler micro-delta. Shaping oracle passed (shaped raw ~88% cap, control ~25x). Honest scope: shaped netns+tc-netem EMULATION, not real WAN (real-hw residual = TASK-207); peer arm no longer a loopback upper bound; near-parity is a STRUCTURAL result on a SYNTHETIC payload (xz ratio is a stated corpus reference, not re-measured); LAN/CPU regime is TASK-203 territory. Bounded gate green: fabric-libp2p build+test(77+ lib), fmt --all, clippy(lib+example) -D warnings, check-no-floats(13 scripts), both --self-tests, 206 regression self-test; disk 82G->79G free. Left In Progress for codex DEEP re-gate; NOT marked Done. NOTE: backlog task-152/176/184 md churn in the tree is from a concurrent session, not this task.

codex DEEP review of 5124e18+0945439: NO-GO. Data/instrument mostly sound but 4 overclaims + a fail-open oracle. F1 CRITICAL (shaped_compress.py:372, measurement.json:12, README:32): headline 'wire ratio' uses provider NarSize/bulk-compressed (16777216/4219763) NOT the authoritative COUNTED transport bodies (16777216/4219766=8388608/2109883) — the NarSize-vs-compressed UNIT TRAP (now 4th recurrence). Fix: headline from per-run wire_raw_bytes/wire_zstd_bytes ONLY; bulk size auxiliary cross-check. F2 CRITICAL (shaped_compress.py:283/440): FAIL-OPEN oracle — a slower-zstd/non-robust mutation sets verdict flags false but _print_report STILL prints 'sign robust' + parity + writes evidence + exits 0 (codex reproduced). Fix: FAIL CLOSED — suppress ALL win/robustness/parity conclusions + evidence write + return non-zero unless every load-bearing flag passes; --self-test the RENDERED output + exit status, not internal booleans. F3 HIGH (shaped_probe.rs:220/230, README:6): probe injects provider addr+PeerId OUT OF BAND + starts timing AFTER dial — NO DHT discovery, EXCLUDES dial/Noise/yamux handshake — contradicts 'discover->fetch->serve' + the 'dial+handshake fixed cost' explanation. Fix: time the real discovery/dial path OR relabel honestly as an already-connected open-stream/fetch measurement + REMOVE the handshake attribution + re-derive the real reason the wall-clock speedup (3.196x) is below the wire ratio. F4 HIGH (README:52/79): claim OBSERVED sign stability (not guaranteed-no-reflip from 3 runs) + structural WIRE-VOLUME near-parity (NOT latency parity); fix 'time shrinks by 3.976x' (wall-clock is 3.196x; wire VOLUME shrinks 3.976x). F5 MED (shaped_compress.py:263): shape-gate EVERY headline run (all 3), not just run 1. Next: fix all, re-gate.

codex NO-GO fixes (43832c0 probe, a2652da instrument, 7ecc14d evidence @ a2652da; superseded 5124e18, removed). All 5 findings addressed; left In Progress for codex re-gate.

F1 (unit trap, 4th recurrence): HEADLINE wire ratio now derived EXCLUSIVELY from per-run COUNTED wire bodies (CountingReader raw wire / zstd wire) = 16777216/4219766 = 8388608/2109883 ~3.976x — like-units, exact rational, never NarSize-vs-bulk. The provider-side bulk compress_zstd frame (4219763 B) is kept ONLY as an auxiliary cross-check (crosscheck_wire_bulk): must agree with the counted zstd body within 1/64 (exact integer compare 64*|d|<=counted); here |diff|=3 B so it passes; a disagreement REJECTS (load-bearing flag wire_bulk_crosscheck_ok), never silently becomes the headline.

F2 (fail-open oracle, load-bearing): report now FAILS CLOSED. finalize() gathers every load-bearing flag (zstd_faster_every_run, margin_dwarfs_noise, all_runs_shape_gated, wire_bytes_consistent, wire_bulk_crosscheck_ok, wire_smaller_every_run); if ANY false -> prints VERDICT: REJECTED (never the win/robustness/parity conclusions), does NOT write the affirmative evidence, exits non-zero. --self-test now asserts on the RENDERED report text AND the exit status (not internal booleans): 6 mutations (slower-zstd, swamped-margin, shaping-removed, run-not-shape-gated, wire-bulk-mismatch, wire-bytes-inconsistent) each render VERDICT: REJECTED, omit the win/parity conclusion, and exit non-zero. Demonstrated live.

F3 (false path/timing label): chose option (b) honest relabel. shaped_probe.rs + docstrings now state the timed window is an ALREADY-CONNECTED open-stream /nar/3 fetch — discovery/dial/Noise+yamux handshake are OUT of the clock (dial completes before t0). Re-derived the real fixed cost IN the window: the request round-trip (open substream + request + first-byte latency ~1 RTT) + stream flow-control ramp, both arms once, payload-independent — the honest reason wall-clock speedup (~3.199x) < wire ratio (~3.976x). Removed the dial+handshake attribution.

F4 (sign/parity language): claims the OBSERVED sign robust (min margin 5253951581 ns ~409x the combined spread 12828396 ns), NOT a guaranteed no-flip from 3 runs. Parity is STRUCTURAL on WIRE VOLUME: wire volume shrinks ~3.976x while measured WALL-CLOCK shrinks ~3.199x — no longer says time shrinks by the wire ratio.

F5 (shape-gate every headline run): gate_shaping() applies the negative-control oracle to the RAW arm of ALL 3 shaped runs (serialized per-run in shaping_oracle.per_run; all_gated), not just run 0.

Discipline: integer/rational only (check-no-floats green, 13 scripts); frozen surfaces untouched (no wire-framing change; swarm.rs unchanged; probe edits are comments only). Bounded gate GREEN under nix develop: fabric-libp2p build + lib test 77/0, cargo fmt --all --check, clippy -p fabric-libp2p --all-targets -D warnings, shaped_compress --self-test, check-no-floats. 3 bounded shaped runs (16 MiB, 20ms/20mbit) accepted. Disk 79G free before/after. NO AI co-author trailers (audited).

codex RE-GATE #1 (43832c0/a2652da/7ecc14d): NO-GO — F5 FIXED, data sound + unchanged, but 3 fixes INCOMPLETE. F1 NOT-FIXED (shaped_compress.py:409): wire-bulk cross-check examines only shaped_runs[0]; a run-2 disagreement is ignored. F2 NOT-FIXED (shaped_compress.py:765): codex reproduced a run-2-only bulk_frame=1000 mutation -> renders ACCEPTED + PEER-VS-UPSTREAM + exit 0 + writes measurement.json; the self-test mutates EVERY run simultaneously so misses a single-run mutation. F1+F2 ROOT: the cross-check AND the fail-closed gate must be PER-RUN over ALL runs; --self-test must mutate a SINGLE run (e.g. run2) and prove REJECTED+exit1. F3 NOT-FIXED (shaped_probe.rs:234): dial().await returns on dial INITIATION not ConnectionEstablished, so Noise/yamux handshake still runs inside the timed open_stream -> the 'already-connected' label is FALSE. Fix: start t0 AFTER SwarmEvent::ConnectionEstablished for the peer (handshake genuinely excluded) so the label is TRUE. F4 NOT-FIXED (README:115): one residual line 'the 3.976x wire reduction shrinks its transfer time correspondingly' -> the prohibited wire-ratio->time conflation; grep ALL evidence for any wire-ratio-as-time. Next: per-run F1/F2 + true-boundary F3 + all-lines F4, re-gate.

codex RE-GATE #1 NO-GO fixes (41f7e0f probe+swarm, 609c20b instrument, 47a55de evidence @ 609c20b; superseded a2652da, removed). All 3 incomplete fixes made COMPLETE + PER-RUN; data (byte counts / counted wire ratio) UNCHANGED.

F1 (per-run bulk cross-check): crosscheck_wire_bulk now takes the run LIST and checks EACH run's provider bulk frame against THAT run's counted zstd wire body; the load-bearing flag keys off the aggregate all_ok, not shaped_runs[0]. The prior run-0-only check ignored a run-2 disagreement.

F2 (fail-closed over ALL runs + per-run self-test): every per-run flag ANDs across all 3 runs, so a SINGLE failing run rejects the whole verdict (no affirmative measurement.json, exit non-zero). --self-test gains a PER-RUN matrix: for each run index {0,1,2}, a single-run corruption (bulk-frame, wire-body-drift, slower-zstd, shape-collapse) renders VERDICT: REJECTED, omits ACCEPTED + PEER-VS-UPSTREAM, exits non-zero. Demonstrated the exact codex escape: run-2-only bulk_frame=1000 -> aux run 2 agree=False -> all_ok=False -> wire_bulk_crosscheck_ok fails -> VERDICT: REJECTED, exit 1 (was: ACCEPTED + PEER-VS-UPSTREAM + exit 0 + wrote json).

F3 (true timing boundary): dial().await returns on dial INITIATION, so the Noise/yamux handshake ran INSIDE the timed open_stream window and the 'already-connected' label was FALSE. Added read-only SwarmHandle::is_connected(peer) (Command::IsConnected -> Swarm::is_connected, additive, no wire framing change); shaped_probe now POLLS is_connected until ConnectionEstablished before setting t0. Discovery+dial+handshake now genuinely OUTSIDE the window; label TRUE. CONSEQUENCE: excluding the handshake from BOTH arms removed a shared fixed cost, so the wall-clock speedup moved 3.199x -> 3740615528/1106901707 ~3.379x (expected + honest; counted wire ratio unchanged). Still below the wire ratio, explained by the in-window request round-trip (~1 RTT) + flow-control ramp.

F4 (wire-ratio->time): grepped ALL evidence + scripts + probe; removed every affirmative 'wire ratio shrinks time by that factor' (README:119 'shrinks its transfer time correspondingly'; README:127 'closes the time gap by the measured ratio'; shaped_compress docstring + printed conclusion). Now stated distinct EVERYWHERE: wire VOLUME shrinks ~3.976x; measured WALL-CLOCK shrinks ~3.379x; NEVER equated. Final grep clean (only hit is the explicit 'NOT a time ratio' disclaimer).

MEASURED (16 MiB compressible nar, ~48ms recovered RTT, 20 mbit, 3 runs, BOTH ends shaped): wire ratio 8388608/2109883 ~3.976x; best wall-clock speedup 3740615528/1106901707 ~3.379x; min margin 5260064795 ns dwarfs spread (5928089 raw + 13014342 zstd) ~278x; shaping oracle 3/3 gated (control RTT 51000 ns, ~28x faster). all load_bearing_flags true.

Bounded gate GREEN under nix develop (CARGO_TARGET_DIR=~/.cache/nix-p2p-target): fabric-libp2p build + example build; cargo fmt --all --check (0); clippy -p fabric-libp2p --all-targets --locked -D warnings (0); cargo test -p fabric-libp2p --lib 77/0; shaped_compress --self-test (per-run matrix bites all 3 indices); check-no-floats (13 scripts, clean). 3 bounded shaped runs accepted. Disk 79G free before/after (no orphaned builds). No AI/co-author trailers (audited); git identity untouched; backlog md's NOT staged. Left In Progress for codex re-gate #2.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE (codex DEEP-gate GO, 3 rounds, 2026-08-15). The owner-prioritized DEMONSTRATION of compression: a LIVE measured raw-vs-zstd libp2p /nar/3 transfer over a tc-netem-shaped veth with BOTH ends shaped — the measured counterpart to TASK-203's idealized model. RESULT: counted-transport wire ratio 8388608/2109883 ~= 3.976x; measured wall-clock speedup ~3.379x (exact rational) on ~48ms RTT / ~2.5MB/s (20mbit) home uplink, 3 runs. The peer's ~3.6x-raw disadvantage vs the xz CDN shrinks by the ~3.976x wire volume to STRUCTURAL near-parity (~0.9x) on a SYNTHETIC compressible payload (xz ratio a stated reference, not re-measured). The wall-clock 3.379x is honestly BELOW the wire ratio (in-window request round-trip ~1 RTT + flow-control ramp, both arms once). HONESTY MECHANISMS (hard-won over 3 codex rounds): headline from COUNTED wire bytes only (provider bulk frame is an auxiliary per-run cross-check that REJECTS on >1/64 disagreement — the NarSize/compressed unit+provenance trap, 5th recurrence, killed); FAIL-CLOSED per-run oracle (any run failing any load-bearing flag -> REJECTED, no measurement.json, exit 1; codex's independent single-run mutation probe = 18/18 bite); shaping negative-control oracle gates ALL 3 runs; the timed window genuinely EXCLUDES discovery+dial+Noise/yamux handshake (is_connected polled before t0 — proven, and it honestly shifted the speedup 3.199->3.379x); OBSERVED sign robust (margin ~278x the spread, integer compare), NOT a guaranteed no-flip; wire VOLUME != WALL-CLOCK stated everywhere. Integer/rational only, no floats in gates; frozen surfaces untouched (swarm.rs +27 additive read-only IsConnected; reused TASK-203 serve + peer_fabric::codec). ENVIRONMENT BOUNDARY: netns+tc-netem EMULATION (a tighter upper bound than loopback, NOT a real WAN with loss/jitter/NAT — that residual is TASK-207). Also spotted + filed a TASK-206 latent bug (shaped_libp2p.py arm-shape KeyError) as TASK-217. Commits: 5124e18,0945439,43832c0,a2652da,7ecc14d,41f7e0f,609c20b,47a55de. Provenance in git notes on 47a55de.
<!-- SECTION:FINAL_SUMMARY:END -->
