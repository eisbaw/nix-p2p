---
id: TASK-154
title: >-
  fabric-libp2p: adversarial resource bounds + packet/source guards
  (sybil/eclipse/amplification; no tracker/LAN/OOB injection)
status: Done
assignee: []
created_date: '2026-08-12 07:55'
updated_date: '2026-08-14 23:02'
labels:
  - libp2p
  - fabric
  - dht
  - security
  - hardening
  - wave-2c
dependencies:
  - TASK-103
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Follow-up to TASK-103 (AC#8 + AC#9). Enforce record/provider/request/response/storage/concurrency/rate/work bounds on the kad path (MemoryStore caps, get_providers fan-out cap via DiscoveryBudget.max_peers, per-query concurrency, put/announce rate) so poisoning/amplification/sybil/eclipse assumptions hold without compromising integrity. Add packet/source guards proving tracker/LAN/implicit-public presets and out-of-band address injection are DISABLED during qualification, with a mutation that enables any substitute making the proof FAIL (oracle bites by mutation). The custom /nix-p2p kad protocol already keeps these nodes off the public IPFS DHT; make that a guarded invariant.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 record/provider/request/storage/concurrency/rate/work bounds enforced and unit-proven on the kad path
- [ ] #2 sybil/eclipse/amplification/poisoning assumptions documented and bounded without compromising integrity
- [ ] #3 packet/source guard proves no tracker/LAN/implicit-public/OOB injection during qualification; a mutation enabling any substitute makes the proof fail
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Includes the TASK-103 mped-architect S4 finding: find_providers enforces the deadline at the async boundary but does NOT cancel the underlying kad query (it runs to its own query_timeout, replying into a dropped receiver - bounded wasted work). Add a Cancel(QueryId) command threading the QueryId back out of SwarmHandle, as part of the work/rate bounds.

IMPLEMENTED on WIP base b0c34fe (commit e0c5326). Per-AC status (verification left to orchestrator gate; status kept In Progress):

AC#1 bounds enforced + unit-proven:
- Store caps: content_store_config() constants are EXPLICIT (STORE_MAX_RECORDS=4096, STORE_MAX_VALUE_BYTES=2048, STORE_MAX_PROVIDERS_PER_KEY=20, STORE_MAX_PROVIDED_KEYS=1024). Distinct from blind defaults: value cap 2KiB (vs 65KiB lib default) is the anti-amplification cap with headroom over the 1024B frozen MAX_PROVIDER_RECORD_BYTES; records 4096 (vs 1024) headroom; providers/key 20 and provided-keys 1024 pinned explicitly (=k / home-node key count). Bite tests (swarm.rs): store_config_carries_explicit_caps_not_blind_library_defaults; store_rejects_oversized_value_but_accepts_a_legitimate_record (ValueTooLarge at ceiling, frozen-size accepted); store_caps_providers_per_key_to_bound_a_sybil_flood (flood of cap+5 distinct providers -> stored==cap).
- FAN-OUT was DECLARED but UNWIRED in the WIP: find_providers received budget but only used budget.deadline; resolve never capped the Phase-2 provider fan-out. FIXED: resolve now takes max_peers and calls new pure cap_fan_out() (deterministic sort+truncate) before the value fetches. Bite tests (directory.rs): fan_out_capped_to_max_peers_when_index_over_budget (100 providers -> 16); _keeps_all_when_index_within_budget; _is_deterministic_across_iteration_order.

AC#2 documented (swarm.rs store-caps block 'ADVERSARIAL MODEL & HONEST LIMITS'): poisoning/amplification/sybil are RESOURCE-bounded only — integrity never depends on the bounds (every fetched record is ed25519-verified + PeerId-bound + monotonic/withdrawal admit-floored; Nix re-verifies bytes). ECLIPSE is explicitly NOT defeated by these bounds (a determined neighborhood-occupying adversary still biases the view; bounds only cap the resource cost). These are source bounds + unit proofs, NOT an adversarial FIELD proof — the multi-node sybil/eclipse swarm demo is deferred to TASK-205 (not attempted here; needs a harness).

AC#3 source/injection guard (scripts/check-public-dht-isolation.py, sibling to check-discovery-no-shortcut.py; wired into just lint): forbidden-token arm bites public /ipfs/kad/1.0.0, /ipfs/lan/kad, bootstrap.libp2p.io, dnsaddr/bootstrap; positive-invariant arm bites REMOVAL of the private /nix-p2p/<scope>/kad marker (catches the quiet Behaviour::new/Config::default() default-preset regression a forbidden-list cannot see, since /ipfs/kad lives inside the library). --self-test proves 4 bites (clean passes; +ipfs-kad fails; +bootstrap fails; -marker fails). Real scan: 15 shipped kad-path src files clean.

S4 cancel: CancelOnDrop RAII unit-proven — armed drop enqueues Command::Cancel{id}; disarm() suppresses (normal completion NOT cancelled); worker Cancel arm (query_mut().finish()+drop pending) panic-free+idempotent for live/unknown id. find_providers deadline path now CANCELS via the guard (stale 'follow-up' comment corrected).

Bounded gate all green: build (fabric-libp2p, daemon-libp2p); clippy -D warnings + fmt; cargo test -p fabric-libp2p (63 lib incl 9 new + all integration; six proven cornerstones green); just independence; full just lint (both guards self-test+scan; check-lock-sources auto-registered the new guard).

NOT a Sybil/eclipse field proof — bounds+guard+unit-proofs only; eclipse field resistance -> TASK-205.

DEEP-gate codex review (gpt-5.6-sol, high) of e0c5326 (full surface 1fbc79b..e0c5326): NO-GO. 4 blockers + 1 medium — all concrete, all confirmed real. Two blockers were INTRODUCED by the fan-out fix itself. Verified clean: 2048-byte cap accepts frozen 1024B record; all four store caps reach production config; no floats; no frozen codec/golden-vector change; just lint + full fabric-libp2p suite green (but those do NOT exercise the guard mutation or the cancel races).

B1 (swarm.rs:942 / directory.rs:149) — fan-out capped AFTER unbounded aggregation+sort: Pending::found accumulates every forged PeerId (HashSet), then copy+sort BEFORE truncate -> O(n) mem, O(n log n) sort in forged count. AC#1 does not bound adversarial WORK, only the Phase-2 fetches. Fix: thread max_peers into GetProviders/Pending, keep a bounded deterministic set during each FoundProviders event.
B2 (directory.rs:293/348) — truncation -> false authoritative Miss: 17 providers, max_peers=16, retained 16 stale + discarded 1 valid => consult_failed stays false => returns Miss (authoritative negative). max_peers=0 => instant Miss for every nonempty index. CROSSES the TCB boundary (hostile/broken peer must cost a retry, never a wrong answer) + poisons negative caching. Fix: carry a `truncated` flag; return Unavailable (retryable), not Miss, when the bounded subset yields no valid result.
B3 (swarm.rs:503/788/283) — abandoned queries not reliably cancelled: CancelOnDrop armed only AFTER QueryId arrives, so a deadline between enqueue and QueryId leaves the query running to normal timeout; and try_send(Cancel) silently drops on a full command channel. S4 work-bound unreliable. Fix: worker finishes immediately when the id-reply receiver is closed; use a lossless cancel path armed before command submission.
B4 (check-public-dht-isolation.py:106/198) — the guard does NOT bite the real mutation: positive marker scans COMMENTS, so swapping the private construction at swarm.rs:1425 for kad::Behaviour::new STILL PASSES (markers linger in comments swarm.rs:1280, daemon-libp2p/src/lib.rs:83). Oracle theater — the "oracle must bite by mutation" failure. Fix: strip comments; require the EXECUTABLE private StreamProtocol -> kad::Config -> Behaviour::with_config wiring; self-test proves the Behaviour::new swap FAILS.
B5/Medium (swarm.rs:1206) — ~8 MiB threat-model doc overclaims: 4096x2048 bounds VALUE-PAYLOAD bytes only, not keys/maps/provider+address records/overhead. Fix: reword as an ~8 MiB value-payload bound; document the other bounded structures separately.

Status: In Progress (NOT Done). Root-fix all 5 + re-gate with codex.

DEEP-gate NO-GO fixes landed (commits c5fc9db fabric-libp2p B1/B2/B3/B5; 8710d74 guard B4). Left In Progress for orchestrator codex re-gate.

B1 (swarm.rs): fan-out bounded AT THE SOURCE. retain_bounded_provider keeps <=max_peers smallest-by-PeerId in a BTreeSet as FoundProviders events arrive -> O(max_peers) memory under a sybil flood, not O(N)+O(N log N) post-hoc sort. max_peers threaded through Command/Pending GetProviders; get_providers now returns ProviderFanOut{providers,truncated,reach}. Deterministic subset == old sort+truncate. Proof: retain_bounded_provider_holds_at_most_max_peers_regardless_of_flood (len<=max_peers asserted after EVERY insert), _keeps_the_max_peers_smallest_deterministically (order-independent, == smallest max_peers), _max_peers_zero, _duplicate_is_not_a_discard.

B2 (directory.rs): truncated flag carried out; classify()+classify_empty_index() return retryable Unavailable(Backend) NOT Miss when empty-and-truncated (retained subset all stale, or max_peers=0 over nonempty index). Only genuinely-empty+non-truncated may Miss. Proof both dirs: classify_unavailable_when_truncated_and_all_retained_stale (a); classify_miss_only_when_healthy_and_empty_and_not_truncated + classify_empty_index_defers_to_reach_only_when_not_truncated (b). Cornerstone negative paths unaffected (budgets 20/32 never truncate their handful).

B3 (swarm.rs): (a) worker finishes query when id_reply.send fails (caller gone before QueryId) - closes enqueue->id race; applied to GetProviders/GetRecord/LocatePeer. (b) cancel moved from best-effort try_send on the BOUNDED command channel (silently dropped when full) to a DEDICATED UNBOUNDED QueryId channel = lossless. Command::Cancel removed; cancel_query helper idempotent/panic-free. Proof: cancel_on_drop_is_lossless_even_when_many_cancels_queue_up (1000 cancels, all delivered in order), cancel_on_drop_fires_when_armed_and_disarm_suppresses.

B4 (check-public-dht-isolation.py): strip_comments() before positive scan (only in-CODE marker counts) + require executable Behaviour::with_config (specific, not bare with_config, so MemoryStore::with_config cannot mask a swapped kad ctor). --self-test now 6 bites incl (5) marker-only-in-comment+Behaviour::new BITES and (6) in-code-marker-but-Behaviour::new BITES. Verified by MUTATION: sed with_config->Behaviour::new on the real swarm.rs -> real scan exits 1.

B5 (swarm.rs): ~8 MiB reworded to VALUE-PAYLOAD-only (4096*2KiB), excludes keys/maps/provider+addr records/overhead; other structures separately bounded. No overclaim.

Bounded gate exits (all 0 unless noted): cargo build fabric+daemon 0; cargo test -p fabric-libp2p 0 (67 lib incl new; six cornerstones green: bootstrap_independence/decentralized_discovery/record_lifecycle/nar_transport/nat_traversal/node_locator_discovery); guard --self-test 0 + real scan 0 + real-mutation scan 1 (correct bite); cargo fmt --check 0; cargo clippy -D warnings fabric+daemon 0; just independence 0 (includes no-floats + shaping). Disk: 120G->116G free (well above 15G floor). Did NOT run just test/e2e (disk).

HONEST LIMIT: B2 truncated-empty maps to Unavailable::Backend(msg) (retryable) rather than a bespoke variant - functionally correct (caller treats all Unavailable as non-absence/retry) but not a dedicated reason code. B4 stripper handles line/block/string/raw/char literals but is not a full Rust lexer (guard's stated obfuscation limit).

codex RE-GATE of e0c5326..8710d74 (the B1-B5 fix commits): NO-GO, but converging. B1 FIXED (retain_bounded_provider evicts largest above max_peers, O(max_peers) mem, subset preserved). B2 FIXED (truncated-empty->Unavailable, genuinely-empty->Miss - the TCB false-Miss boundary closed). B4 FIXED for the basic mutation (comments stripped; Behaviour::new swap fails self-test). REMAINING: B3 NOT-FIXED (swarm.rs:581/895 - after id_reply.send(id) succeeds the id can sit BUFFERED while the caller drops before polling id_rx + arming CancelOnDrop; worker inserts Pending with no cancel signal. Bounded by kad_query_timeout so not an unbounded leak, but the S4 cancel AC is not honestly met). B5 NOT-FIXED (swarm.rs:1340 - reworded doc still calls attacker-controlled provider/address storage a 'smaller addend' with NO byte bound; overclaim remains). F2 BLOCKS (check-public-dht-isolation.py:257/273 - marker hits and Behaviour::with_config aggregated GLOBALLY + checked independently, so a 2nd unrelated with_config masks the Behaviour::new mutation; self-test misses the global-noise case -> AC#3 oracle bypassable). F1 does not block (pre-existing, filed TASK-214). Next: fix B3+B5+F2 at root, re-gate.

codex RE-GATE residual fixes F2+B3+B5 (commits df61fff guard, 6ad89ac swarm.rs). Left In Progress for orchestrator codex re-gate.

F2 FIXED (check-public-dht-isolation.py): AC#3 positive-invariant no longer aggregates the private-kad marker and Behaviour::with_config GLOBALLY+INDEPENDENTLY. Now tracked PER FILE and required CO-LOCATED (both in the SAME file); ctor regex scoped to the fully-qualified kad::Behaviour::with_config (not bare with_config nor Behaviour::with_config), so MemoryStore::with_config / an unrelated behaviour's with_config cannot stand in. Verdict names missing-marker vs missing-wiring vs un-colocated distinctly. Self-test adds case (7) GLOBAL-NOISE: private marker in one file + kad ctor swapped to Behaviour::new + an UNRELATED kad::Behaviour::with_config in ANOTHER file MUST FAIL. Proven by MUTATION on the real tree: real ctor->Behaviour::new bites (wiring arm, exit 1); same swap + a probe with_config file bites (co-location arm, exit 1); clean tree + self-test both exit 0. Kept the forbidden-token arm + comment-stripper (codex-confirmed good).

B3 FIXED (swarm.rs, S4 AC honestly met): closed the buffered-id window. After id_reply.send(id) succeeds the id can sit BUFFERED while the caller is dropped BEFORE polling id_rx + arming CancelOnDrop; worker inserted Pending with no cancel signal, query ran to kad_query_timeout. Root fix: worker detects caller-abandonment on the RESULT reply channel via Pending::reply_is_closed() — checked EAGERLY at the top of on_query (next kad event reaps) AND on a periodic 1s ABANDONED_QUERY_SWEEP_INTERVAL sweep (backstop for a stalled walk emitting no further event). Both cancel the kad walk + drop the entry via the existing idempotent/panic-free cancel_query. Selection split into pure abandoned_query_ids() for network-free proof. Tests: reply_is_closed_tracks_caller_abandonment_across_every_variant; abandoned_query_in_the_buffered_id_window_is_selected_for_reaping_not_the_live_one (dropped rx reaped, live waiting caller untouched). This CLOSES the window at root (not documented-as-residual): eager reap for active queries, sweep bounds a stalled query's reap latency to <=1s (far under the 30s query_timeout).

B5 FIXED (swarm.rs): removed the overclaim. The provider/address store is no longer called a 'smaller addend' — reworded to 'separately bounded by STORE_MAX_PROVIDERS_PER_KEY * STORE_MAX_PROVIDED_KEYS (20*1024=20480 records), each size-limited by libp2p's internal per-record address cap; byte size NOT quantified here, NO smaller/ordering claim'. Also dropped the matching 'dominant' claim in the RECORD COUNT bullet + STORE_MAX_RECORDS doc. Integers only.

Bounded gate (all exit 0 unless noted): cargo build fabric+daemon 0; cargo test -p fabric-libp2p 0 (69 lib incl 2 new B3 tests; six cornerstones GREEN: bootstrap_independence 2/decentralized_discovery 1/record_lifecycle 5/nar_transport 15/nat_traversal 3/node_locator_discovery 2; B1/B2 retain/classify tests green); guard --self-test 0 (now 7 bite cases incl F2 global-noise) + real scan 0 + real-mutation scans exit 1 (correct bite, both arms); cargo clippy --locked -D warnings fabric+daemon 0; cargo fmt --all --check 0; check-no-floats.py 0. Disk 115G free before+after (>15G floor). Did NOT run just test/e2e (disk). F1 untouched (pre-existing TASK-214).

codex RE-GATE #2 (df61fff F2 + 6ad89ac B3+B5): B3 FIXED (receiver-closure abandonment detection; live callers not reaped; eager+1s-sweep both via idempotent cancel_query). B5 FIXED (integer bound 20*1024=20480, no ordering claim, bytes unquantified honestly). 1s sweep ACCEPTABLE (one idle wake/sec + O(pending) scan, honest <=1s latency). F2 STILL NOT-FIXED: co-location is only FILE-level, so a same-file decoy kad::Behaviour::with_config + real ctor swapped to Behaviour::new still passes (check-public-dht-isolation.py:287). This is the 3rd narrower source-scan bypass (global->file->same-file) = non-converging regex whack-a-mole (see memory hand-rolled-edge-cases-defer-behind-trust-boundary). DECISION: stop patching the regex; the ROOT fix is a SEMANTIC oracle binding to the actually-constructed kad protocol (unbypassable by source decoys), regex kept as cheap secondary lint. Dispatching final convergent cycle. B1/B2/B3/B5 all codex-confirmed fixed; F2 is the sole remaining item.

F2 FINAL FIX (commit 6672274): path 1 (SEMANTIC oracle) taken — libp2p-kad 0.46.2 exposes kad::Behaviour::protocol_names() on the constructed runtime object, so path 2's honest-scope fallback was NOT needed. Extracted the SINGLE production kad ctor into build_kad_behaviour (Node::start calls it; no behaviour change). New test swarm::tests::kad_speaks_only_the_private_scoped_protocol_never_the_public_ipfs_dht calls that same ctor and asserts protocol_names() IS /nix-p2p/<scope>/kad AND shares nothing with the library default preset (derived at runtime from bare_kad(), so no forbidden literal hardcoded). BINDS to the runtime object, not source text: the same-file decoy that bypassed the regex 3x (global->file->same-file) cannot fool it. Proven by MUTATION: swapping with_config->Behaviour::new in build_kad_behaviour makes the test FAIL observing protocol_names()==[/ipfs/kad/1.0.0] (exit 101). check-public-dht-isolation.py reframed HONESTLY as the CHEAP SECONDARY lint (forbidden-token+comment-strip+co-location arms unchanged, still self-test 0 + real scan 0). Bounded gate all exit 0: build fabric+daemon; cargo test -p fabric-libp2p (70 lib incl new oracle; six cornerstones green bootstrap_independence 2/decentralized_discovery 1/record_lifecycle 5/nar_transport 15/nat_traversal 3/node_locator_discovery 2); guard self-test 0 + real scan 0 + mutation scan (n/a, semantic now) ; clippy --locked -D warnings 0; fmt --check 0; check-no-floats 0. Disk 115G free before+after (>15G floor). B1/B2/B3/B4/B5 UNTOUCHED. Left In Progress for orchestrator codex re-gate.

codex FINAL re-gate #3 (6672274 semantic oracle): F2 NOT-FIXED — but for a LEGITIMATE reason + no regression (extraction preserves caps/config/timeout/mode; 101 tests + both scans + no-floats green). Gap: the semantic test calls build_kad_behaviour() DIRECTLY (swarm.rs:1974), decoupled from production's call site (swarm.rs:1727). Production could regress (Node::start inlines Behaviour::new) while the helper stays a passing decoy -> both test and scan pass. The test binds to a HELPER, not the production path. This is the 4th narrower F2 gap (global->file->same-file->helper-vs-production) = non-converging heuristic. CONVERGENT ROOT FIX (fail-fast, binds to the real path): Node::start itself asserts its CONSTRUCTED kad protocol_names() == the private scoped protocol AND is disjoint from the library default preset, and REFUSES TO START otherwise. A regression then cannot boot — unbypassable except by deleting the check (adversarial developer, OUT of the project's peer-focused trust boundary per README). Dispatching. If codex finds a round-5 contrived-adversarial gap, ARBITRATE via the trust boundary (codex stricter than project TCB) + close honestly. B1/B2/B3/B4/B5 remain codex-confirmed fixed.

codex FINAL re-gate #3 F2 fix (commit eb8b727): bound the AC#3 public-DHT-isolation invariant to the PRODUCTION PATH. Root fix per orchestrator brief: Node::start, AFTER .build(), calls assert_private_kad_isolation(swarm.behaviour().kad.protocol_names(), &scope, peer_id) on the ACTUAL constructed kad object the worker will run (swarm.rs Node::start, right after the SwarmBuilder .build()), and returns NodeError::Build (REFUSE TO START) unless (a) the private /nix-p2p/<scope>/kad/1.0.0 protocol IS advertised AND (b) the advertised set is DISJOINT from the library default preset. The default preset is derived at RUNTIME by default_preset_protocol_names() (a throwaway kad::Behaviour::new) so the forbidden /ipfs/kad literal is never hardcoded (guard forbidden-token arm not tripped; real scan still 15 files clean). This closes the 4th F2 gap (helper-vs-production): the semantic test bound to build_kad_behaviour, leaving Node::start free to regress to Behaviour::new with the helper staying a passing decoy. Now a regression cannot BOOT. Unbypassable except by deleting the check (adversarial developer, out of the peer-focused trust boundary).

Tests bind to production, not a helper: production_isolation_invariant_accepts_the_real_private_kad (real ctor protocol set passes the invariant Node::start runs); production_isolation_invariant_bites_the_default_preset_regression (feeds the default-preset object's protocol_names to the SAME production function -> refuse-to-start Err; the BITE); production_node_start_passes_the_public_dht_isolation_invariant (#[tokio::test] drives the REAL Node::start -> Ok for the private scope, so the invariant does NOT reject the legit node). Existing semantic test + check-public-dht-isolation.py secondary lint UNCHANGED. B1/B2/B3/B4/B5 UNTOUCHED.

PROVEN BY MUTATION on the real tree: swapped build_kad_behaviour's kad::Behaviour::with_config -> kad::Behaviour::new; Node::start then returned Build('kad public-DHT isolation invariant VIOLATED (refusing to start): ... advertised ["/ipfs/kad/1.0.0"] ... the node must not boot.') and production_node_start_passes FAILED (node refused to boot); reverted cleanly (149 insertions, 0 deletions).

Bounded gate (all exit 0 unless noted): cargo build fabric+daemon 0; cargo test -p fabric-libp2p 0 (73 lib incl 3 new; six cornerstones green bootstrap_independence/decentralized_discovery/record_lifecycle/nar_transport/nat_traversal/node_locator_discovery); guard --self-test 0 (7 bite cases) + real scan 0 (15 files); cargo fmt --all --check 0; cargo clippy --locked -D warnings fabric+daemon 0; check-no-floats.py 0. Disk 115G->114G free (>>15G floor). Did NOT run just test/e2e (disk). Commit eb8b727. Left In Progress for orchestrator codex re-gate.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
TASK-154 DELIVERED (codex DEEP-gate GO, 2026-08-15). Adversarial resource bounds + source/injection guards for the libp2p-kad discovery path.
AC#1 anti-sybil/amplification: explicit kad MemoryStore caps (STORE_MAX_VALUE_BYTES=2048 load-bearing anti-amplification, MAX_RECORDS=4096, MAX_PROVIDERS_PER_KEY=20=k, MAX_PROVIDED_KEYS=1024) + BOUNDED consumer fan-out (retain_bounded_provider: smallest-by-PeerId eviction during aggregation, O(max_peers) memory, not O(n) sort-then-truncate).
S4 query work-bound: Command::Cancel + CancelOnDrop RAII + worker reaps caller-abandoned queries (reply_is_closed eager + 1s sweep) — the buffered-id window closed at root.
AC#3 public-DHT isolation: a FAIL-FAST PRODUCTION INVARIANT — Node::start validates swarm.behaviour().kad.protocol_names() is the private /nix-p2p/<scope>/kad and disjoint from the library default preset, refusing to boot otherwise. A regression to Behaviour::new/Config::default CANNOT START. (Evolved from a source-scan regex that codex bypassed 3x global->file->same-file, then a semantic-helper test codex showed decoupled from production, to this production-bound invariant.) Regex guard kept as honest cheap secondary lint.
AC#2 threat-model doc: honest value-payload byte bound, no overclaim; explicitly NOT a field eclipse proof.
Convergence through a 4-round codex cross-model DEEP gate: NO-GO(4 blockers) -> NO-GO(3) -> NO-GO(1) -> GO. The gate caught real defects the same-model self-cert missed (an unwired fan-out cap, a false-Miss TCB-boundary violation, a cancel race, a non-biting guard). 73/73 fabric-libp2p lib tests + six cornerstones green.
Honest residuals FILED (not hidden): F1 deterministic-smallest-PeerId targeted-key censorship vector (pre-existing) -> TASK-214; adversarial multi-node sybil/eclipse FIELD proof (needs a harness, env-blocked here) -> TASK-205. These bounds cap the RESOURCE COST of an attack; integrity never depends on them (records ed25519-verified + PeerId-bound; Nix re-verifies sig+NarHash).
Commits: b0c34fe c5fc9db e0c5326 df61fff 6ad89ac 6672274 eb8b727. Verification provenance in git notes --ref=verification on eb8b727.
<!-- SECTION:FINAL_SUMMARY:END -->
