---
id: TASK-276
title: >-
  Relax lan_isolation to admit provably-private LAN ranges for zero-config
  same-pin cross-host SERVING (TASK-278 Part B)
status: Done
assignee: []
created_date: '2026-08-19 22:18'
updated_date: '2026-08-20 09:23'
labels:
  - usability
  - privacy
  - follow-up
dependencies:
  - TASK-278
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-273 defaults a LOOPBACK libp2p listen under a bare --profile lan-share because lan_isolation_or_refuse (TASK-102 stopgap) refuses any non-loopback/non-link-local listen for a no-allowlist lan-share (a routable/wildcard listen is treated as 'reachable by strangers'). Consequence: a zero-config lan-share node can DISCOVER same-pin peers over mDNS and FETCH from them, but can only SERVE to loopback/same-host peers. Genuine cross-HOST same-pin serving currently requires the trusted-key allowlist door (public-announce) or an explicit routable --libp2p-listen paired with that allowlist. Decide whether lan-share should admit private-LAN ranges (is_private: 10/8, 172.16/12, 192.168/16) as LAN-isolated for SERVING, given org/LAN same-pin is the honest first product. This is privacy-sensitive (a routable listen without allowlist authorization serves unauthorized content to the LAN), so it needs an explicit call and likely couples to the announce-authorization model. Out of scope for TASK-273.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 multiaddr_is_lan_only (daemon-libp2p/src/lib.rs:980) admits loopback + link-local + PROVABLY-PRIVATE (IPv4 is_private 10/8,172.16/12,192.168/16; IPv6 ULA fc00::/7) for lan-share; still REFUSES global/routable, wildcard 0.0.0.0/::, DNS names; public-share+no-allowlist unchanged
- [x] #2 Bare cross-host --profile lan-share with NO --libp2p-listen FAILS LOUD (auto-resolve + if-addrs DROPPED): 'a cross-host --profile lan-share needs its LAN address; pass --libp2p-listen /ip4/<your-LAN-ip>/tcp/0'. An EXPLICIT provably-private listen is admitted and serves cross-host (both binaries)
- [x] #3 Operator disclosure (both binaries): printed AFTER bind (full bound multiaddr from listen_addrs) but BEFORE the /nar serve gate activates; drops the categorical public-isolation claim -> honestly states same-scope Kad publication is not yet LAN-confined (a dual-homed same-network peer could propagate keys beyond the LAN, TASK-280); under announce-after-fetch discloses it serves every fetched path, not a fixed set
- [x] #4 Cross-host biting e2e in the DEFAULT just-e2e set (E2E_FAST, 14 scenarios): two lan-share nodes with EXPLICIT private --libp2p-listen on distinct container IPs, B fetches FROM A cross-host (upstream.nar==0, byte-identical); negative controls wildcard/global/circuit each FAIL LOUD asserting the guard MESSAGE + no serve/listen line before refusal; guard mutations RED-on-revert
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
RE-SCOPED (Mark-emulator after codex round-2 NO-GO): 276 ships the honest INBOUND cross-host LAN delta only; the egress-transitivity isolation GUARANTEE is split to TASK-280 (blocking the PRD guarantee, not 276's feature). KEEP FIX#1 (positive-grammar/circuit reject, codex-verified closed) + FIX#2 (guard precedes bind/serve both binaries, codex-verified closed). FIX#B: DROP auto-resolve (resolve/select_default_private_listen + if-addrs dep) -> bare no-listen lan-share fails loud (one-flag). FIX#3: disclosure honesty -> order guard->bind->PRINT->serve-gate; full bound multiaddr; composite parity; drop categorical public-isolation claim + name TASK-280; honest announce-after-fetch wording; add a one-paragraph known-gap note to PRD.md referencing TASK-280. FIX#4: e2e explicit-listen + guard-message neg-controls (wildcard/global/circuit) + cross-host in E2E_FAST. Re-gate: just e2e-full + qa+mped+codex; codex must confirm the disclosure no longer overclaims.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Folded into TASK-278 (its default-listen / cross-host-serving AC). Keep for the lan-isolation-relaxation detail; sequence under the supply task.

TASK-276 AC#1-3 implemented + AC#4 e2e proven (commits a5d6bfc, 8f87ddd). AC#1: multiaddr_is_lan_only admits loopback+link-local+provably-private (ip_is_provably_private: IPv4 is_private 10/8,172.16/12,192.168/16; IPv6 ULA fc00::/7 via prefix test); STILL refuses global/routable, wildcard 0.0.0.0/::, DNS-name, CGNAT 100.64/10; public-share+no-allowlist unchanged; shared predicate so composite daemon gets parity. AC#2: select_default_private_listen (pure, unit-tested) + resolve_default_private_listen (if-addrs enumeration; if-addrs 0.15 already in Cargo.lock via libp2p-mdns/if-watch, promoted to direct dep = NO new supply-chain crate) auto-resolves ONE private-LAN listen for a bare lan-share; 0 or >1 (multi-NIC/docker0/VPN) fails LOUD, never guesses. AC#3: SERVING-on-priv/tcp/port disclosure from actual bound listeners. GUARD MUTATION-PROVEN (unit): drop is_private admission -> private-admit test RED; admit global -> public-refused test RED. AC#4 scenario libp2p-lan-share-cross-host-serve PASS 9/9 (76.1s): two bare lan-share nodes on distinct private container IPs, B peer-fetches target FROM A cross-host byte-identical upstream.nar==0; A logged auto-resolve /ip4/10.211.34.11/tcp/0 + SERVING disclosure port 36659; negative controls wildcard+global fail loud. Gates: cargo test daemon-core/daemon-libp2p/daemon/fabric-libp2p 40 result-groups ok 0 fail; cargo fmt clean; ruff clean; nix-instantiate parse OK. Committed local master only, no push, no AI trailer. NOT marking Done - orchestrator owns Done after qa+mped+codex + full-e2e DEEP re-gate. Honest limits: AC#2 auto-resolve is daemon-libp2p only (composite daemon has no lan-share mDNS-default zero-config path); down interfaces not filtered from enumeration (conservative -> more likely to fail loud); IPv6 ULA disclosure formats as ip6 addr in the tcp/port line.

AC#4 TOPOLOGY CHOICE (transparency for DEEP gate): used the SANCTIONED first option in the plan (extend the existing multi-container bridge topology with distinct private container IPs) rather than scripts/shaped_link*.py netns. Each daemon runs as its OWN podman --network container (own netns, own 127.0.0.1) on ONE multicast-capable bridge 10.211.34.0/24 with distinct --ip (A=.11, B=.10) - genuinely cross-host (separate netns, real L2 dial A<-B), the SAME mechanism the existing libp2p-mdns-bootstrap/mdns-scope-isolation/lan-share-zeroconfig scenarios use. New class Libp2pLanShareServeTopology + scenario, registered in the DEFAULT SCENARIOS set. shaped_link.py (netem link-shaping) was unnecessary since no bandwidth/latency shaping is under test here - only cross-host reachability.

codex DEEP security review VERDICT_NO_GO (real holes; literal IP classification confirmed sound + public-share still allowlist-gated). #1 CRITICAL: multiaddr_is_lan_only ignores all protocols except IP/DNS (lib.rs:1047) so a /ip4/<rfc1918>/.../p2p-circuit is admitted -> a no-allowlist lan-share can reserve through a dual-homed relay and an INTERNET peer reaches it via the circuit -> reject P2pCircuit; positively validate allowed direct-listen grammars. #2 CRITICAL: the no-allowlist guard runs AFTER fabric bind + active serve gate (guard failure -> RefusePublication at main.rs:1138/1890, then serve() at server.rs:85, then lan_share_or_refuse in the seed/store branches) -> a wildcard/global+seed provider binds all interfaces + a queued exact-key request can be admitted in the window, repeated per supervisor restart -> run the guard ONCE before fabric/listener construction and propagate the witness. #3 HIGH: resolve_default_private_listen discards iface name/state/type -> silently binds VPN-ULA / docker0 / down iface / collapses two ifaces same addr -> does NOT prove the intended LAN iface. #4 HIGH: global e2e neg-control uses 8.8.8.8 (EADDRNOTAVAIL regardless of guard) -> not attributable; use a bindable public addr in-netns + assert guard message. #5 MED: disclosure TCP-only (QUIC admitted no disclosure) + circuit -> false NOT-internet-reachable claim. #6 MED: if-addrs direct edge activates FFI iface-enum in listen-selection TCB (was target-gated); correct the misleading Cargo.toml comment. PLUS gate gap: cross-host scenario is in SCENARIOS but NOT E2E_FAST -> just e2e never ran it. Routing fix spec to Mark-emulator.

codex NO-GO closed (FIX #1/2/3/5/6 + #4 gating). Commits 6e5d120 (rust) e86c810 (e2e). NOT Done - orchestrator owns Done after qa+mped+codex full-e2e re-gate.

FIX #1 CRITICAL multiaddr_is_lan_only: fail-OPEN _ => {} replaced by fail-CLOSED positive grammar - EXACTLY one loopback/link-local/RFC1918/ULA IP literal + one DIRECT transport the SHIPPED swarm builds. QUIC IS shipped (fabric-libp2p/src/swarm.rs .with_tcp(...).with_quic()), so allowed set = {Ip[46]+Tcp, Ip[46]+Udp+QuicV1}. Rejects P2p, P2pCircuit, Dns*, Ws/Wss, Memory, over-long. Biting: circuit_listen_is_refused, relay_hop_listen_is_refused, + extended classify test.

FIX #2 CRITICAL guard hoisted BEFORE fabric/listener build on BOTH binaries: new pure provider_publication_decision(cfg)->PublicationPlan called once at top of install_provider, propagated with ? so a wildcard/global/circuit no-allowlist provider aborts before ANY listener binds. Witness (LanShare, Copy) threaded into seed/store/announce-after-fetch branches via PublicationPlan::Lan(_); authority derived by plan.announce_authority(). Stopped degrading no-allowlist to RefusePublication (removed that unused import both crates). PublicationPlan added to lib + re-exported through daemon. Biting unit: publication_decision_aborts_before_any_fabric (both crates).

FIX #3 HIGH select_default_private_listen now takes InterfaceAddr {name, ip, has_v4_broadcast}, not bare IpAddr. Qualify iff provably-private, not loopback/link-local, and (v4) iface carries a broadcast addr (excludes point-to-point tun/wg WITHOUT a name blocklist; if-addrs sets broadcast only on IFF_BROADCAST). Ambiguity keys on DISTINCT iface NAME -> two NICs sharing one literal are Ambiguous, not collapsed (Ambiguous now carries (name,ip) pairs). Documented v6-ULA residual (no v6 broadcast; mitigated - mDNS multicast does not traverse a p2p link). Biting: two_ifaces_same_literal, docker0+lan, point_to_point_excluded (wg0 no-bcast + eth0 -> Resolved eth0), single clean NIC + single v6 ULA resolve.

FIX #5 disclosure: prints FULL bound multiaddr from listen_addrs() (fixes malformed v6, covers QUIC); dropped categorical NOT-internet-reachable; non-categorical reword + do-not-DNAT warning. FIX #6 Cargo.toml: honest note that promoting if-addrs to direct dep activates its FFI getifaddrs enumeration on the live provider path (small real TCB add), bounded by unsafe_code=forbid + pure-selector rule.

FIX #4 e2e: added libp2p-lan-share-cross-host-serve to E2E_FAST (14 scenarios, stale count fixed). Neg controls now assert the GUARD MESSAGE on wildcard AND global (FIX #2 => guard before bind, so 8.8.8.8 EADDRNOTAVAIL cannot masquerade), a NEW circuit neg-control (bites FIX #1 end-to-end), and a log-ABSENCE oracle (no SERVING/serving line) on all three. Positive SERVING regex updated to full-multiaddr form.

GATES: cargo test daemon-core/-libp2p/daemon/fabric-libp2p 903 ok 0 fail; fmt clean; ruff clean; nix-instantiate parse OK. Security mutations proven RED-on-revert then reverted: circuit fail-open -> circuit_listen_is_refused RED; name-dedup collapse (dedup_by_key ip) -> confidence_rule RED; no-abort decision -> publication_decision_aborts RED. Heavy e2e (background, once): libp2p-lan-share-cross-host-serve PASS 10/10 (77.2s) - 0-upstream cross-host positive + all 3 neg controls fail loud w/ guard msg + no SERVING line. Disk 72G free, no orphaned pods/builds.

HONEST LIMITATION: the e2e log-ABSENCE oracle is a genuine regression guard (confirms the refusal path prints no serve disclosure and bites FIX #1 circuit via the guard message) but does NOT by itself bite the exact FIX #2 before-vs-after-bind ORDERING: with the guard reinstated in the announce branches the abort still precedes the SERVING/PROVIDER-serving lines, so no serve line would leak either way. FIX #2 ordering is proven by the unit test (decision Errs, no fabric constructed) + the ? at the top of install_provider before build_libp2p_provider_source. FIX #2 auto-resolve/disclosure remain daemon-libp2p-only (composite daemon has no zero-config lan-share path); the guard-hoist itself IS applied to both binaries.

codex re-review VERDICT_NO_GO (round 2). VERIFIED CLOSED: FIX#1 grammar airtight (circuit/relay/dns/ws/global/wildcard rejected; direct tcp+quic admitted), FIX#2 no-allowlist decision precedes supply/listener/serve-gate on BOTH binaries (witness threaded; no queued-request window), FIX#4 neg-controls attributable + cross-host in the 14-scenario fast gate. REMAINING gate-breakers: #1 CRITICAL - the ISOLATION PREMISE is false: mDNS-discovered + Kad-returned + Identify addresses enter Kademlia UNFILTERED (swarm.rs:2475/2413), announce does transitive start_providing/put_record (announcer.rs:466), and /nar/4 serves over ANY established connection incl provider-ORIGINATED (swarm.rs:2694, libp2p bidirectional). Exploit: a dual-homed same-v1 Kad node on the LAN also joined to public peers bridges the provider to the public DHT; a public peer learns the key and fetches over the outbound connection, never traversing the private listener. codex: "may predate the parser rewrite" but invalidates the premise used to relax the guard. Closure needs a LAN-non-transitive publication substrate + route/connection provenance (filter mDNS/Kad/Identify addrs; restrict serving to LAN connections). #2 HIGH: resolver still admits docker0/TAP-VPN/down/ULA singletons (IFF_BROADCAST insufficient); need up+default-route attribution OR drop auto-resolve. #3 MED disclosure timing + composite prints generic not bound-multiaddr. #4 LOW FFI comment (workspace unsafe-forbid does not constrain dep-internal unsafe). Routing to Mark-emulator: is #1 a 276 blocker or a pre-existing architectural task; resolver harden-vs-drop.

RE-SCOPE round (codex verified FIX #1 grammar + FIX #2 guard-before-bind CLOSED; egress-transitivity split to TASK-280 as pre-existing). Commits ea6a555 (rust FIX #B + FIX #3 + PRD) 31f38f7 (e2e FIX #4). NOT Done - orchestrator owns Done after qa+mped+codex full-e2e re-gate.

FIX #B DROP auto-resolve: deleted resolve_default_private_listen + select_default_private_listen + DefaultPrivateListen + InterfaceAddr + the main() auto-resolve block; removed if-addrs direct dep from daemon-libp2p/Cargo.toml (Cargo.lock: if-addrs now transitive-only via if-watch, no longer a daemon-libp2p dep); FIX #6 comment dissolved with the dep. A bare cross-host lan-share with EMPTY --libp2p-listen FAILS LOUD both binaries (daemon-libp2p: check_runtime_preconditions lan-share-specific message 'a cross-host --profile lan-share needs its LAN address; pass --libp2p-listen /ip4/<your-LAN-ip>/tcp/0'; composite: enriched the existing --libp2p-provider requires --libp2p-listen message). EXPLICIT provably-private listen still admitted (FIX #1 unchanged) + serves cross-host. Biting: bare_lan_share_without_listen_fails_loud (mutation: weaken the precondition to skip LanShare -> RED).

FIX #3 disclosure honesty + sequencing (both binaries + PRD): new shared lib helpers - lan_serving_disclosures() builds one line per PRIVATE bound listener as the FULL multiaddr (fixes v6, covers QUIC), NON-categorical (dropped 'NOT reachable from the public internet'), names the still-open same-scope-Kademlia gap (TASK-280), warns Do not DNAT/port-forward, and states the announce-after-fetch served-scope ('not a fixed set'); disclose_then_activate_serve() SEQUENCES disclosure BEFORE serve-gate activation. install_provider on BOTH binaries reordered: guard -> bind -> read listen_addrs -> DISCLOSE -> activate /nar serve gate (+ prints '<bin>: /nar serve gate active') -> announce. Composite prints the SAME disclosure (gated on PublicationPlan::Lan). PRD.md new risk #13. Biting: lan_serving_disclosures_are_honest_and_full_multiaddr (mutation: re-add 'not reachable' -> RED), disclosure_precedes_serve_gate (mutation: activate-before-disclose in the sequencer -> RED).

FIX #4 e2e: both cross-host nodes now pass EXPLICIT --libp2p-listen /ip4/<container-ip>/tcp/0; dropped auto-resolved log assertions; assert the corrected disclosure (routes-to-this-address wording, no not-reachable, TASK-280, do-not-forward) + disclosure-precedes-serve-gate log ORDER; wildcard/global/circuit neg controls unchanged; scenario stays in E2E_FAST.

GATES: cargo test daemon-core/-libp2p/daemon/fabric-libp2p 905 ok 0 fail; fmt clean; ruff clean; nix-instantiate parse OK; workspace builds WITHOUT if-addrs (no dangling import). Mutations proven RED-on-revert then reverted (FIX#B precondition-weaken; FIX#3 serve-before-disclose; FIX#3 re-added not-reachable). Heavy e2e (background, once): libp2p-lan-share-cross-host-serve PASS 11/11 (78.5s) - explicit-private-listen admitted + 0-upstream cross-host + FIX #3 disclosure/sequencing checks + all 3 neg controls fail loud w/ guard msg + no serve line. Disk 54G free; no orphaned pods/builds.

HONEST LIMITATIONS: (1) TASK-280 (address-filtering/serve-provenance/kad-scope confinement) NOT touched - the public-internet isolation guarantee is not held end-to-end until it lands; the disclosure + PRD #13 state this. (2) The composite daemon's provider startup now ALSO emits the disclosure + '/nar serve gate active' and moves serve-gate activation after the disclosure; I only ran the cross-host scenario, NOT the composite-daemon libp2p scenarios (s7/s8/etc). Functionally serve-gate still activates before announce so peer fetches are unaffected, but any composite e2e asserting exact provider-startup log content should be re-checked in the DEEP re-gate. (3) disclosure_precedes_serve_gate is proven at unit level via the shared sequencer AND at e2e level via log-order; both bite the serve-before-disclose mutation.

codex v3 VERDICT_NO_GO but EXPLICITLY code-sound ("removal mechanically clean; both binaries fail loud w/o listener; private TCP/QUIC admitted; disclosure before serve gate; no pre-disclosure window; verdict is surviving/new OVERCLAIMS not the arbitrated TASK-280 defect"). Residuals = honesty-wording only: #1 HIGH operator.rs:111 labels lan-share "isolated LAN/private substrate" (printed before the qualified disclosure; preflight exits w/o the TASK-280 caveat); e2e honesty assert only greps literal "not reachable" -> misses semantic equivalents. #2 HIGH disclosure "every store path this node fetches" is served -> budget-limited + skips on failure -> "eligible verified paths up to the budget". #3 MED security comments lib.rs:975/1021/1053/1123/1475 still claim provably/genuinely-isolated / no-public-substrate. #4 MED PRD #13 says bare lan-share binds+serves (auto-resolve removed -> fails loud) + "disclosure honest" (contradicted). #5 LOW stale auto-resolve narratives in e2e comments. Final wording round + strengthen the e2e honesty gate to catch overclaim keywords.

HONESTY-WORDING round (codex confirmed CODE sound; remaining NO-GO was overclaim wording only, no behavior change). Commit c3fb1f0. NOT Done - orchestrator owns Done after full-e2e re-gate.

Per-item status:
1 DONE operator.rs describe() lan-share label reworded (drops 'isolated LAN/private substrate' -> 'serve + announce to same-scope LAN peers on a provably-private listen (public-internet isolation NOT yet enforced end-to-end - see TASK-280)'). ONE edit covers preflight (operator.rs:1054 prints describe()) AND both runtime startup lines (daemon-libp2p/main.rs:1602 + daemon/main.rs:2697 both print describe()).
2 DONE disclosure served-scope under announce-after-fetch reworded to the accurate guarantee ('eligible, successfully-verified fetched paths, up to the configured announce budget - NOT every fetched path'); unit test updated to assert the accurate tokens + reject the old overclaim.
3 DONE security doc-comments softened: LanReachability.listen, ip_is_provably_private, multiaddr_is_lan_only rationale, lan_isolation_or_refuse, PublicationPlan::Lan, AnnounceAfterFetchDoor::Lan - each now describes the LISTEN-address guarantee only + references TASK-280 for the egress/publication gap. Also the 3 refusal-remedy messages ('isolated LAN announce' -> 'no-allowlist LAN announce') and the announce_provider_seeds comment ('genuinely-isolated' -> 'no-allowlist LAN-witnessed').
4 DONE PRD.md risk #13 corrected: a bare lan-share now FAILS LOUD without --libp2p-listen (not 'binds and serves'); the label/comments were overclaiming and are corrected (stated accurately, not 'the disclosure is honest' full-stop).
5 DONE e2e_harness.py stale 'auto-resolves a private listener' narratives (topology docstring, zeroconfig comment 2563, await_server_announce die-message, SCENARIOS registration comment) corrected to the explicit-listen reality.

Strengthened honesty GATE (e2e scenario): now (a) asserts the TASK-280 caveat + 'Do not DNAT/port-forward' are PRESENT in the SERVING disclosure LINE itself, and (b) rejects overclaim keywords ['isolated lan','private substrate','not reachable from the public','not reachable from the internet','provably isolated','genuinely isolated','no public substrate'] anywhere in A's serving/startup output. MUTATION PROVEN (standalone replication of the exact gate logic): corrected output passes; re-adding the old 'isolated LAN/private substrate' label flips it RED (caught 'isolated lan','private substrate').

GREP SWEEP result (converge the tail): fixed all non-iroh isolation overclaims in daemon-libp2p/daemon/daemon-core src incl witness test comments ('provably/genuinely isolated' -> 'no public-reach signal' in daemon-libp2p main.rs:1992/2065 test-doc + assert msg, lib.rs:3027/3051 test docs, daemon main.rs:3236/3243 composite guard test). DELIBERATELY LEFT: daemon/main.rs:537 + 2985 iroh offline/LAN-scope 'genuinely isolated' - a separate legacy transport whose OFFLINE scope IS genuinely local (no network egress); dragging TASK-280 (a libp2p-Kademlia concern) into iroh-scope docs would be inaccurate cross-wiring. The lib.rs:1171/3317 occurrences are META-comments explicitly DISCLAIMING the phrase ('does NOT claim not-reachable'), left as-is.

GATES: cargo test daemon-core/-libp2p/daemon/fabric-libp2p 905 ok 0 fail; fmt clean; ruff clean; nix-instantiate parse OK. Cross-host e2e RE-RUN (assertion changed, so ran once) PASS 12/12 (77.4s) - strengthened honesty gate green on real output; all 3 neg controls still fail loud w/ guard msg + no serve line. Disk 54G free; no orphaned pods/builds. No behavior change (wording + one test-assertion + PRD only).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DELIVERED (narrowed, honest scope): a no-allowlist --profile lan-share can serve cross-host on an EXPLICIT provably-private --libp2p-listen. multiaddr_is_lan_only positively validates the listen grammar (admits only loopback/link-local/RFC1918/ULA + direct TCP or UDP+QUICv1; REJECTS global/wildcard/DNS/relay-circuit/ws/memory) - codex-verified airtight. The no-allowlist isolation guard runs ONCE before fabric/listener/serve-gate construction on BOTH binaries (witness threaded) - codex-verified no bind-before-guard window. Auto-resolve DROPPED (unsafe interface heuristic; if-addrs removed) -> a bare lan-share without --libp2p-listen FAILS LOUD (one-flag, never a wrong guess). Operator disclosure prints the full bound multiaddr AFTER bind but BEFORE the serve gate, drops all categorical public-isolation claims, and names the tracked gap (TASK-280); a strengthened e2e honesty gate mechanically rejects overclaim keywords.

Gate: full just e2e 14/14 PASS (libp2p-lan-share-cross-host-serve 12/12 - explicit-listen cross-host serve upstream.nar==0 byte-identical; wildcard/global/circuit negative controls fail loud with the guard message + no serve line); cargo test 905 passed/0 failed; fmt/ruff/nix-parse clean. DEEP-gated qa+mped+codex across 4 codex rounds; codex (cross-model) caught: scope-creep silent-drop, a relay-circuit LAN bypass, a guard-after-bind window, and honesty overclaims - all real defects mped GO'd past. Final wording-tail arbitrated (code+security codex-verified sound; residuals fixed+swept+gate-enforced).

HONEST LIMITATION (split to TASK-280, High/irreversible): the lan-share public-internet isolation GUARANTEE is not held end-to-end - egress transitivity (unfiltered mDNS/Kad/Identify ingestion + bidirectional /nar serving) lets a dual-homed same-network peer bridge to the public DHT. Pre-existing (listener-independent), cannot fire by default (no default public swarm at HEAD), disclosed to the operator + PRD risk #13.

Commits: a5d6bfc 8f87ddd 6e5d120 e86c810 ea6a555 31f38f7 c3fb1f0.
<!-- SECTION:FINAL_SUMMARY:END -->
