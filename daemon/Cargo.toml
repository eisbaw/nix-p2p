[package]
name = "daemon"
description = "nix-p2p product daemon: localhost Nix binary-cache substituter"
version.workspace = true
edition.workspace = true

# Deliberately dependency-free at scaffold time. The HTTP stack, async runtime
# and NarinfoSource/NarSource module traits are task-4's decisions and are
# left unconstrained here on purpose.
[dependencies]

[lints]
workspace = true
